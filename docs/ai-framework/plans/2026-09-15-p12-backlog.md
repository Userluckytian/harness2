# P12 待办与交接（终端界面新一轮 + 未合并分支处理）

> **谁该读这份**：接手 harness2 终端（CLI）后续工作的同事或 AI 代理。
> **读法**：先读 §0 现状 → 按 §A 处理未合并分支（**必须先做**）→ §B 真机验收 → §C/§D/§E 排期 → §F 是可直接粘贴的开工提示词。
> **上游文档**：`docs/HANDOFF.md`（总交接入口）· `docs/tui-parity/matrix.md`（差异清单）· `docs/ai-framework/plans/2026-09-15-phase-p11-tui-polish.md`（刚完成的阶段）· `docs/issue-log/OPEN.md`（未关闭事项索引）

---

## 0. 现状（2026-09-15 收工快照）

### 0.1 做了什么

| 阶段    | 内容                                                                                                                                                     | 状态                |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| **P10** | 删除 CLI 旧终端渲染路径（React/ink 壳）：代码/测试/依赖/现行文档**零残留**；建立「双 TUI 抓屏对照台」`scripts/tui-parity/`，产出与 grok 的逐场景差异报告 | ✅ 完成，**未合并** |
| **P11** | 按对照报告修 **1 项 P0 + 6 项 P1**（`/help` 错位、输入区可见性、候选两列、时间戳/耗时/token、工具行人类化、忙碌态反馈、冷启动引导卡）                    | ✅ 完成，**未合并** |

### 0.2 分支与提交

```
main                          48fca36  （落后：本地还有 2 个文档提交未 push）
feat/phase-p10-next-only      ...      P10（含 P11 计划提交）
feat/phase-p11-tui-polish     cb477d2  ← 当前 HEAD，P11 全部工作，叠在 P10 之上
```

- 两个分支**都未 push**（按项目规矩需人类授权）；`feat/phase-p11-tui-polish` 相对 main 共 **29 个提交**。
- 阶段内提交格式：`<gitmoji><type>(scope): 中文描述`；编排者/子代理分工见 `docs/ai-framework/workflow-delegation.md`。

### 0.3 闸门实测（2026-09-15，P11 收口时）

| 闸门                                      | 结果                                                                                                                                                                                                                         |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm -r build` / `pnpm -r typecheck`     | 6/6 包 Done                                                                                                                                                                                                                  |
| `pnpm lint`                               | **0 error / 46 warnings**；`prettier --check .` 全绿                                                                                                                                                                         |
| `pnpm --filter harness2 test`             | **93 files / 1575 passed + 2 skipped**（P10 起点 1520 → P11 +55 条）                                                                                                                                                         |
| `@harness2/core`                          | 1232 passed + 2 skipped（**与基线一致 ⇒ core 零改动**）                                                                                                                                                                      |
| `@harness2/gateway` / `ui-shared` / `web` | 40 / 70 / 36 passed                                                                                                                                                                                                          |
| `@harness2/desktop`                       | 94/96 files、930 passed + 1 skipped；**2 个红文件为预存环境问题**（`test/serve-manager.test.ts` 需 `127.0.0.1:46213` 空闲；`test/settings/models/*.test.tsx` 的 jsdom `node:` 报错——已用 `main` 同环境对照证明与本阶段无关） |
| 越界检查                                  | `packages/{core,gateway,ui-shared,desktop,web}` 改动 **0 文件**                                                                                                                                                              |

### 0.4 怎么自己复验（5 分钟）

```bash
git checkout feat/phase-p11-tui-polish
pnpm install && pnpm -r build
pnpm --filter harness2 test          # 期望 93 files / 1575 passed + 2 skipped
pnpm lint                            # 期望 0 error + prettier 全绿
# 看界面（Windows Terminal 真机）：
node packages/cli/dist/index.js chat --provider mock
#   依次试：/help、敲 /、输入文字、Enter 一轮、Ctrl+C
# 抓屏对照（可复跑，零成本）：
python -m venv scripts/tui-parity/.venv
scripts/tui-parity/.venv/Scripts/python.exe -m pip install -r scripts/tui-parity/requirements.txt
scripts/tui-parity/.venv/Scripts/python.exe scripts/tui-parity/run.py --side ours
```

---

## A. 必须先做：合并与 CI（阻塞其余工作）

> 未合并前，main 上仍是「旧壳存在 + 无对照台」的旧状态，后续开发会踩空。

| #   | 步骤                                    | 命令 / 动作                                                                                                                  | 备注                                |
| --- | --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| A1  | 推送两个分支                            | `git push -u origin feat/phase-p10-next-only` 然后 `git push -u origin feat/phase-p11-tui-polish`                            | **需人类授权**（项目规矩）          |
| A2  | 看 CI 七 job                            | GitHub Actions：3 平台 `test` + 3 平台 `build-desktop` + `pages`                                                             | 本地无法代验（`gh` 未认证）         |
| A3  | 先合 P10 再合 P11                       | `git checkout main && git merge --no-ff feat/phase-p10-next-only` → 推 → CI 绿 → 再 `--no-ff` 合 `feat/phase-p11-tui-polish` | **顺序不可颠倒**（P11 叠在 P10 上） |
| A4  | 合完整批一次推送并回查 run `conclusion` | —                                                                                                                            | 见 `docs/HANDOFF.md` 分支策略行     |
| A5  | 删除已合分支（本地 + 远程）             | `git branch -d` / `git push origin --delete`                                                                                 | 项目惯例：远程只留 `main`           |

**风险提示**：CI 的 `desktop` job 在 3 平台跑测试——本机那 2 个 jsdom 红文件在 CI 上是否复现未知（P9 时 CI 全绿，倾向不复现）。若 CI 红在这两处，先修测试环境再合。

---

## B. 真机验收清单（只能人眼/人手，AI 代不了）

> 脚本抓屏（`scripts/tui-parity`）覆盖不到：鼠标、IME、系统粘贴、真机字体度量、真机观感。
> 命令：`node packages/cli/dist/index.js chat --provider mock`（Windows Terminal / 最新 Node 22）

| #   | 项                     | 怎么验                                | 期望                                                   | 结果 |
| --- | ---------------------- | ------------------------------------- | ------------------------------------------------------ | ---- |
| B1  | `/help` 面板观感       | 输入 `/help`                          | 两列对齐、无重叠（P11 修的是"文字互相覆盖"）           | ⬜   |
| B2  | 输入区锚点与占位       | 空态看光标位置；输入文字看 `❯` 前缀   | 一眼知道在哪打字；提示符不被提交                       | ⬜   |
| B3  | 消息时间戳在真机字体下 | 跑一轮对话                            | 行尾右对齐时间戳不跳位、不与正文重叠                   | ⬜   |
| B4  | 引导卡在小画布是否被裁 | 把窗口缩到很窄（<80 列）后重启        | 引导卡不裁坏、不挡住输入                               | ⬜   |
| B5  | 引导卡开关             | `HARNESS2_NO_WELCOME=1 node ... chat` | 不显示欢迎卡                                           |      |
| B6  | 候选列表两列           | 敲 `/`                                | 命令 + 灰说明两列；窗口极窄时退化为单列                | ⬜   |
| B7  | 鼠标/选择复制          | 拖选转录文本、Ctrl+C 复制             | 不误触应用快捷键                                       | ⬜   |
| B8  | IME 中文输入           | 切换到中文输入法打字                  | 组合期间不闪烁、不错字                                 | ⬜   |
| B9  | 系统粘贴               | Ctrl+V 多行文本                       | 换行归一正确、不误提交                                 | ⬜   |
| B10 | alt-screen 进出        | 进入/退出（`/exit`）                  | 退出后终端滚动缓冲干净、光标恢复                       | ⬜   |
| B11 | minimal 模式           | `/minimal`（或 config 设 minimal）    | 引导卡/输入区在 minimal 下正常                         |      |
| B12 | 会话切换后状态行       | 带一轮对话后 `/new`                   | `ctx` 不得残留上一会话数字（应为估算百分比或 `ctx —`） | ⬜   |

> 已有历史真机清单（更细的键位项）：`docs/ai-framework/plans/2026-09-13-terminal-real-machine-checklist.md`；`docs/issue-log/OPEN.md` 中「next 壳真机验收」行。

---

## C. 功能与体验待办（P12 排期，来自对照报告）

> 证据原话与截图见 `docs/tui-parity/matrix.md` 改进清单（编号对应）。

| #   | 项                                                             | 为什么                                                                                                                                                                                                                                                                                                                                                                                                                        | 建议做法                                                                                                                                                                                                                                                                                               | 涉及文件                                                                                                                           | 验收                                                                        |
| --- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| C1  | 状态行缺**模型名**与 **git 分支**（matrix #8）                 | grok 顶部给「分支 + 目录 + token」，多模型/多 worktree 时是刚需                                                                                                                                                                                                                                                                                                                                                               | 状态行扩展字段；分支从 `.git/HEAD` 读（失败则省略）                                                                                                                                                                                                                                                    | `src/tui/next/chat-screen.ts`、`next-shell.ts`                                                                                     | 抓屏帧含模型名/分支；无仓库时优雅省略                                       |
| C2  | **会话 id 常显**（#9）                                         | 首行 `会话: 2026…（新建）` 对用户无意义（grok 用标题）                                                                                                                                                                                                                                                                                                                                                                        | 改为会话标题（core 已有自动标题 `title.json`）；无标题时降级为「新会话」                                                                                                                                                                                                                               | 同上 + core 标题 API（只读）                                                                                                       | 首屏不出现长 UUID                                                           |
| C3  | **未知命令语义**（#10）                                        | grok 会把未识别 `/xxx` 交给技能/命令路由执行；我们直接报错                                                                                                                                                                                                                                                                                                                                                                    | **先定性**：确认 grok 语义后再决定；倾向保留报错 + 追加"最接近命令"建议                                                                                                                                                                                                                                | `src/tui/next/next-shell.ts`（命令分发）                                                                                           | 行为与文档一致                                                              |
| C4  | `/rewind <prompt>` 别名（#11）                                 | grok 没有 `/undo` `/redo`，是 `/rewind <prompt>`（官方 README）；从 grok 迁来的用户肌肉记忆                                                                                                                                                                                                                                                                                                                                   | 增加别名指向既有 undo 流（映射到最近 N 轮）；**不改 undo/redo 语义**                                                                                                                                                                                                                                   | `src/tui/next/next-shell.ts` 命令表                                                                                                | 别名可回退到指定历史 prompt                                                 |
| C5  | 子任务卡片补**耗时/步数**（#12，**我方优势项，保持并放大**）   | 我们的 `⏺ Subagent "…" 完成` 比 grok 的普通工具行更清楚                                                                                                                                                                                                                                                                                                                                                                       | 卡片补耗时与子会话步数；保留跳转入口                                                                                                                                                                                                                                                                   | `src/tui/next/**`（子任务视图）、`cards/`                                                                                          | 抓屏帧含耗时/步数                                                           |
| C6  | **背景色**（用户消息底色、候选选中行底条）与**逐字符匹配高亮** | 用户 2026-09-15 提问「对话/指令面板有没有背景色、匹配项会不会变色」。现状：**全链路 fg-only**——`CellBuffer` 只有 `chars/widths/fg/linkIds`（无 bg 属性位），`DiffPresenter` 只发射前景 SGR，全仓无 `48;2` 背景序列、无 SGR 7 反色；选中/active 一律**换前景色**（`scrollback.SELECTION_FG` 亮青近似反色、`composer.candActiveFg`）。grok 同样无背景色（参考实现也是 fg-only）→ 属**超出参考实现的自选增强**，不是 parity 缺口 | ① 逐字符匹配高亮：改动小、可先做——`filterCommands` 额外返回命中下标，候选渲染按段换色（不碰属性模型）；② 背景色：需给 `CellBuffer` 加 bg 通道 + `DiffPresenter` 发射 `48;2` SGR + 主题加 bg 槽位，**动 presenter 属性模型**（`renderer/cell-buffer.ts:75-76` 已登记该取舍），改动面较大 → 建议单独阶段 | `renderer/cell-buffer.ts`、`renderer/diff-presenter.ts`、`next/composer.ts`、`next/next-shell.ts`(filterCommands)、`next/theme.ts` | 帧断言 + 抓屏（`.txt` 不含颜色，需人工看图或加逐格 fg/bg 断言）；真机看观感 |

---

## D. 技术债（已登记，非缺陷；顺手修即可）

> 全部来自 P11 阶段级独立审查（`docs/ai-framework/plans/2026-09-15-phase-p11-tui-polish.md` 审查节）；同一批也登记在 `docs/issue-log/OPEN.md`。

| #   | 项                                                                                                          | 位置                                         | 建议                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| D1  | live 帧时间戳用「落帧墙上时钟」而非事件 `ts`                                                                | `src/tui/next/next-shell.ts` `dispatch()`    | 若要与磁盘重放完全一致，可在 `chat-setup.ts` 写入口转发 `assistant/message.ts`（需 item-id 映射） |
| D2  | 忙碌态 `↓N` 是「本步输出」非回合累计（grok 是回合总量）                                                     | `next-shell.ts` `busyDownTokens`             | 改为按 turn 累加，或文案改成「本步」                                                              |
| D3  | token 分母为固定近似 `DEFAULT_CONTEXT_WINDOW`（未用模型声明窗口）                                           | `next-shell.ts`                              | 从 provider 配置取声明窗口，取不到再降级                                                          |
| D4  | piped 汇总行新增耗时段仅被 `toContain` 弱断言覆盖                                                           | `src/render.ts`、`test/chat.test.ts`         | 补精确断言                                                                                        |
| D5  | 宽度表双轨：`projection.ts` 用 `displayWidth`（string-width），`renderer/cell-buffer.ts` 用内置 `charWidth` | 两文件                                       | 长期合并为单一事实源（当前已证对帮助文本一致）                                                    |
| D6  | `\t` 一律降级为 1 空格（代码缩进观感变歪）                                                                  | `renderer/cell-buffer.ts` `sanitizeCellChar` | 可改为「进网格前把 tab 展开为空格」                                                               |
| D7  | 抓屏证据产物（`.log.json`/notes）会进 prettier 门禁                                                         | `docs/tui-parity/images/**`                  | 每批次跑一次 `prettier --write`，或加 `.prettierignore` 一行（二选一）                            |
| D8  | 会话日志写入依赖「换行即提交」语义（旧坑，勿动）                                                            | `packages/core/src/session/writer.ts`        | 改前先读其测试                                                                                    |

---

## E. 对照台遗留（补齐后差异面才算完整）

| #   | 项                                  | 说明                                                                                                                                                                       |
| --- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | `E1 审批弹窗`、`L2 模型报错`        | 场景标 `defined_only`：mock provider 自动放行/不报错，复现不了 → 需真机或真实 key 时补                                                                                     |
| E2  | `L1 工具失败`（grok 侧）            | 现有剧本会让 grok 走技能/命令路由（`Writing command…`），未触发失败 → **剧本要重写**：让它直接读一个不存在的文件                                                           |
| E3  | `K 组` 逐格折行核对                 | 110×30 / 160×40 双方帧都有，但没逐格比完                                                                                                                                   |
| E4  | `J1 队列` 我方 `01-queued` 帧为空帧 | 抓帧时机早于首帧渲染（非缺陷）；要么调大等待，要么改用 `T6-busy-*` 那对做证据                                                                                              |
| E5  | grok 侧模型口径                     | 对照用的是 grok 默认模型（当前为本地 40080 端点 `big-pickle`，单轮 ~3.7s）。若默认模型再变，抓帧耗时特征会变 —— 建议给 `run.py` 加 `--grok-model` 参数固定（**尚未实现**） |
| E6  | `hermes-agent` 参考仓仍未拉取       | 历史遗留：下轮任何涉及 `H-*` 条目的工作开工前必须先补拉（见 `docs/issue-log/OPEN.md`）                                                                                     |

---

## F. 给接手同事/AI 的开工提示词（可直接粘贴）

---

你是 harness2 项目的新接手者。仓库：`D:/AI_Projects/harness2`（Windows + Git Bash，Node ≥22、pnpm 11、Python 3.12）。

**必读顺序**：`docs/HANDOFF.md` → 本文件（`docs/ai-framework/plans/2026-09-15-p12-backlog.md`）→ `docs/tui-parity/matrix.md` → `docs/issue-log/OPEN.md` → `AGENTS.md`（协作规范）→ `docs/ai-framework/workflow-delegation.md`。

**现状**：两个分支（`feat/phase-p10-next-only`、`feat/phase-p11-tui-polish`）已在本地完成并全绿，但**未 push、未合 main**。

**你的第一件事**：按本文件 §A 处理合并与 CI（需人类授权 push；顺序先 P10 后 P11，`--no-ff`）。不要跳过。

**然后**：§B 真机验收一轮（人眼，AI 代不了）→ §C 从 C1 开始排期 → §D 技术债顺手修 → §E 补对照遗留。

**铁律**（违反即失败）：

1. `packages/core` / `packages/gateway` 是冻结区，**行为零改动**；任何改动须走解冻窗口（见 `docs/HANDOFF.md` §0 与 `docs/API-STABILITY.md`），并保持导出面基线 diff = 0 或走基线更新流程。
2. `packages/ui-shared/src/styles/*.css` 的 `--ink` 是**配色 token（颜色名）**，不是被删的库，**禁止误改**。
3. 终端只有一个 TUI（自绘 `src/tui/next/**`）；**不要再引入 React/ink**，旧壳已彻底删除。
4. 所有终端宽度计算走 `displayWidth`/`charWidth`，禁止把 `.length` 当显示宽度（P11 的 P0 就出在这类地方）。
5. 禁止伪造数据（耗时/token/文案一律真实来源或如实降级 `—`）。
6. 改完必须跑：`pnpm -r build` + `pnpm --filter harness2 test` + `pnpm lint`（prettier 也要绿）。
7. 不 push（除非人类授权）；历史归档目录（`docs/issue-log/*.md` 除 `OPEN.md`、`docs/diary/*`、`docs/research/*`、已完成的旧 plans、`spike/`）不改不删。

**交卷要求**：分支名、提交列表、每项验收命令的真实输出、残留风险；如遇阻塞（如需改 core 才能完成）**停下报告**，不要自行扩大范围。

---

## G. 明确不做（避免范围蔓延）

- ❌ 恢复/兼容旧终端壳（已彻底删除，不复活）
- ❌ 让 AI 生成 UI 截图冒充真机截图（`docs/screenshots/` 三张图必须是真机截取）
- ❌ 为对齐 grok 而引入插队立即发等**我们尚无**的功能并伪造成"已有"
- ❌ 未获授权就 push / 发布 / 改 CI 配置（`.github/workflows/**` 属编排者独占）
