# 每日日志 2026-09-06

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 关键结论 / 决策 / 风险与待办 / 下一步。

## 今日完成

1. **项目启动**：`git init`；集成 ai-framework（OpenCode 项目级：6 agents、8 commands、AGENTS.md、CODE_REVIEW.md、architecture.md、coding-standards.md、docs/ai-framework/*），验证无未替换占位符。
2. **参考资源盘点与拉取**：
   - 本地：`refs/grok-build`（xAI Rust TUI，Apache-2.0）、`refs/hermes-agent`（Nous Research，MIT）
   - 新 clone：`refs/deepseek-harness`（DeepSeek 官方 TS monorepo，MIT，9080 文件）
   - 本机四应用探查（Tokeny / Hermes / opencode / ZCode）：技术栈、开源度、数据目录 schema 全部摸清
3. **深度分析**（4 个并行代理，全部完成）：
   - grok-build：rewind 机制（prompt 即 checkpoint、三模式、快照冲突检测、dry-run）、scheduler actor、崩溃一致写入
   - hermes-agent：记忆双轨制（文件记忆 + provider 插件）、写入 gate 三态、nudge 后台 review、QQ 官方 Bot API v2 适配器、cron at-most-once
   - deepseek-harness：Cordis 一切皆插件、轨迹事件日志（Model-visible ⟺ logged）、Profile/Bundle 组合、19 个 GH Actions workflows
   - opencode：`/undo` `/redo` 官方语义查证（投影截断，不动底层数据）
4. **产出文档**：
   - `docs/research/2026-09-06-reference-analysis.md`（调研报告，含来源路径与待查证清单）
   - `docs/ROADMAP.md`（26 项功能清单，P0/P1/P2，含 6 项架构决策与"明确不做"）
   - `docs/ai-framework/plans/2026-09-06-phase-1-session-core-trajectory.md`（阶段 1 计划草案 + 交接提示词）

## 关键结论

1. **撤回恢复 vs 分叉的长期答案**：三家（opencode/grok/dsh）数据层趋同——append-only 日志 + 撤回只是投影变化。推荐：undo/redo 为主（P1，opencode 语义），fork 为辅（P1.5，同一机制免费获得），grok 的三模式 + 冲突检测 + dry-run 作为 undo 增强。底层一套，交互两皮。
2. **记忆系统**：hermes 方案完整且 MIT——文件记忆 + 硬预算 + 写入 gate（off/询问/自动 = 用户要的开关）+ nudge 后台 review；Tokeny 的记忆访问统计作为后期质量闭环。
3. **Tokeny 已确认闭源**（Electron，GitHub, Inc. v1.5.7），不做逆向集成；其"按场景分模型"的 `{channelId, model}` schema 可直接借鉴。
4. **技术栈推荐**：TypeScript/Node monorepo + Electron 桌面 + JSONL/SQLite 存储 + QQ 官方 Bot API 网关（依据：dsh 验证 TS 多形态、本机四家应用三家 Electron、grok 证明 Rust 体量风险）。
5. **轨迹优先可行**：dsh 的"Model-visible ⟺ logged"不变量 + append-only 代际文件是可照抄的地基，且快照回放测试让 CI 无需 API key。

## 决策记录

| 决策 | 内容 | 状态 |
|------|------|------|
| D1 | TS/Node monorepo | 推荐待确认 |
| D2 | Electron + React 桌面 | 推荐待确认 |
| D3 | JSONL 事件日志 + SQLite 索引 | 推荐待确认 |
| D4 | 自研轻量插件总线（不引 Cordis） | 推荐待确认 |
| D5 | 会话内核与 UI 解耦 | 推荐待确认 |
| D6 | 独立文件快照（不依赖 git） | 推荐 |
| — | 每日日志放 `docs/diary/YYYY-MM-DD.md` | ✅ 生效 |

## 风险与待查证

1. QQ 官方 Bot API 对个人开发者的当前开放范围 → 集成前实测（P2 前）
2. Windows NTFS fsync 语义 → 阶段 1 写入器测试覆盖
3. pnpm 在 Windows 的符号链接 → D1 确认时定（备选 npm workspaces）
4. Electron WebContentsView 多窗口内存表现 → P1 原型验证

## 下一步

1. ~~等待用户确认 ROADMAP 决策 D1–D5（技术栈）~~ → **已确认**（同日晚间）：技术栈按推荐定案；跨端 = 跨 OS + 跨形态；QQ 开放平台已有注册机器人。
2. 确认后执行阶段 1 计划（会话内核 + 轨迹最小闭环）→ **已开工**：architecture.md 重写为 harness2 真实架构（原文件是 ai-framework 模板残留的其他项目内容，已替换），建分支 `feat/phase-1-session-core`，按计划 Task 1–5 执行。

### 阶段 1 执行记录（当日追加，分支 `feat/phase-1-session-core`）

- Task 1 monorepo 骨架（797f654）：pnpm workspaces 四包（core/cli/desktop 占位/gateway 占位）+ vitest 5 + tsc strict（NodeNext）。
- Task 2 事件类型 v1 + 单写者写入器：9 类事件、目录锁（陈旧锁接管）、fsync、崩溃残行截断恢复、open 续写 seq 连续（5 测试）。
- Task 3 读取器与投影：rewind/marker 追溯遮蔽（影子事件保留可导出）；`AnySessionEvent` 改判别联合修 payload 收窄（6 测试）。
- Task 4 轨迹 CLI：`harness2 traj`（时间线/工具树/`--json`/`--all`，纯 ASCII 防乱码）；根 test 脚本改为 build+test 保证集成测试拿新 dist（2 测试）。
- Task 5 快照回放测试：确定性 fixture（限流失败 + rewind 场景）→ 黄金投影断言 + Model-visible⟺logged 校验 + writer 重放无分叉（5 测试）。

**验证证据**（全部实跑）：`pnpm -r typecheck` exit 0；`pnpm test`（build+test）18/18 通过；`node packages/cli/dist/index.js traj packages/core/fixtures/demo-session` 渲染正确、中文无乱码；`git ls-files` 无敏感文件。

**执行中发现并修复的问题**：
1. architecture.md 是 ai-framework 模板残留的他项目内容（碳卫星 Vue 前端）→ 重写为 harness2 真实架构。
2. 投影首版单遍扫描会把 rewind 标记之后的新分支事件误遮蔽 → 改两遍扫描（先遮蔽集后投影）。
3. 轨迹渲染多行工具输出会撑破单行 → 换行折叠为 `\n` 字面量。
4. shell 工作目录停在子包导致根脚本没跑、dist 过期 → CLI 集成测试失败，根 test 脚本改为 `pnpm -r build && pnpm -r test`。

**状态**：阶段 1 自验通过；独立验收（`/accept-phase`）待做，故计划状态保持「实施中」。

## Release note 素材（积累）

- 初始化项目：ai-framework 集成（阶段化计划驱动 + 提交前审查）
- 完成四参考项目深度调研与 26 项功能路线图

---

## 同日第二段：切换子代理协作模式 + 阶段 1 独立审查与验收

**新工作模式（用户确认，已写入 AGENTS.md 与 docs/ai-framework/workflow-delegation.md）**：实现/测试/审查由子代理承担；编排者（主会话）只做计划、分发、验收、文档与全局把控。

**执行记录（新模式首次完整跑通）**：
1. 独立审查代理（只读）审查阶段 1 全部 diff → **verdict: pass-with-fixes**：验收表 5 项字面通过，但发现 3 个可实测复现的 P1 数据缺陷（空行致静默截断合法事件、无换行尾行致文件反向扩展+锁泄漏、payload 零校验致 traj 崩溃）+ 5 组 P2 建议。
2. 修复代理按清单修复 3 个 P1 + 新增 15 个回归测试（29e5058、f23dc85），33/33 测试全绿。
3. 编排者验收：亲自重跑 typecheck（0 错）、pnpm test（33 过）、traj 渲染、敏感文件（0）、append-only 抽查（truncate 仅崩溃恢复路径）→ **阶段 1 验收通过**，计划状态改「已完成」。
4. P2 项按新约定录入 `docs/issue-log/OPEN.md`，带入阶段 2。

**过程中发现的异常（需用户知悉）**：工作树中出现一批非本会话产生的 ai-framework 脚手架更新（新增 vision-analyst 代理、new-agent 命令、scripts/vision-bridge.py、plan-layering.md、问题日志约定、coding-standards 重写等）。两个子代理均否认创建；结合内容判断为用户侧（opencode/技能更新）所为，已按原样保留并入库，并把被其覆盖的「工作模式」章节重新补回 AGENTS.md。若来源不符预期请告知。

**Release note 素材**：阶段 1 交付并验收通过（会话内核 + 轨迹 CLI + 33 测试）；确立编排者/子代理协作模式与问题日志机制。

---

## 同日第三段：阶段 2 执行记录（分支 `feat/phase-2-agent-loop-tools`）

> 实现代理按 `docs/ai-framework/plans/2026-09-06-phase-2-agent-loop-tools.md` 完整执行 Task 1–7。

**交付**：Agent loop（turn/step 状态机）+ 工具系统（契约/注册表/执行器）+ 基础工具集 6 件 + MockProvider + 三平台 CI 骨架 + 消化阶段 1 审查 P2 项。

**按 Task 执行记录**：

- Task 1（137b711）：`provider/types.ts` ChatProvider 缝 + `provider/mock.ts` 脚本化 MockProvider（流式分片/错误注入/请求记录/signal 短路），8 测试。
- Task 2（10d7a77）：`tools/{types,registry,executor}.ts`——注册即 disposer、审批三态缝、超时与外部信号组合（raceAbort 兜底不自觉观察 signal 的工具）、波次调度（unsafe 独占/连续 safe 并行/lockKey 链式串行），16 测试。
- Task 3（b2a2dda）：`tools/predefined/` 六工具（bash 32KB 截断、read 2000 行上限、write/edit 原子写+路径锁、glob fast-glob 上限 1000、grep 优先 rg 回退 JS 扫描）+ tool-tree fixture（中文内容/二进制），25 测试。
- Task 4（dd1d935）：`agent/{types,loop}.ts`——模型请求上下文唯一来源=日志投影（`buildChatMessages`），无内存旁路；12 测试含**Model-visible ⟺ logged 不变量测试**（测试内独立回放日志逐步重建，与 mock.requests 全等）。另生成 `fixtures/loop-demo` 会话供手工验证。
- Task 5（cd76da1）：`.github/workflows/ci.yml` 三平台 matrix；本地用临时 `yaml` 包（仓库外 temp 目录安装，不留依赖）完成语法与结构校验；OPEN.md 登记「CI 待远程验证」。
- Task 6（0638862）：P2-1（rewindToSeq 写入口强校验 + 读侧越界告警）、P2-3 部分（fsync 默认路径冒烟测试）、P2-5（tool/result 带 callId、`── turn` 标头、exhaustive 兜底、CLI 一行友好错误）；4 测试。
- Task 7：文档整备（architecture.md / ROADMAP P0-4/5/6 状态 / 本日志 / HANDOFF 快照 / issue-log）。

**执行中发现并修复的问题**：

1. executor 首版 lockKey 串行用轮询实现（死锁风险）→ 改 promise 链式串行。
2. 超时只对自觉观察 signal 的工具有效 → 增加 raceAbort 竞速短路（不遗漏底层 rejection）。
3. rg 默认只尊重 .gitignore，无 git 仓库的目录会搜进 node_modules → 给 rg 加显式 `--glob '!node_modules' '!.git'`，与 JS 回退扫描行为对齐。
4. `runTurn(dir)` 对新会话需自动 create（原实现只会 open）→ 目录分支按日志是否存在选择 create/open。
5. 取消瞬间工具即使已完成也会被记为 cancelled（raceAbort 语义）→ 测试预期按"取消即取消"对齐，append-only 落盘 ok:false。

**验证证据（全部实跑）**：`pnpm test`（build+test）→ core 94 passed + 1 skipped（环境变量门控的 loop-demo 生成器）+ cli 3 passed；`pnpm -r typecheck` 两包 Done；`node packages/cli/dist/index.js traj packages/core/fixtures/demo-session` 与 `.../loop-demo` 渲染符合预期（turn 标头/callId 正确显示）；ci.yml YAML 解析校验通过。

**测试数**：33 → 98（新增 65；core 95 + cli 3）。

**残留**：CI Actions 真实运行待用户建远程并授权 push（OPEN.md）；`── turn` 标头的 Windows Terminal 实机渲染列入手工验收。

---

## 同日第四段：阶段 2 独立代码审查修复（分支 `feat/phase-2-agent-loop-tools`）

> 修复代理按独立审查清单（3 P1 + 8 P2，P2-6 评估不修）执行；测试口径更正：此前文档所记「98 全绿」实为 97 passed + 1 skipped（跳过项是 `H2_GEN_LOOP_DEMO` 门控的 fixture 生成器）。

**P1 修复**：
1. **CI 三平台必现失败**：`pnpm/action-setup@v4` 的 `version: 11` 与 `packageManager: pnpm@11.13.0` 严格比较冲突 → 删除 version 输入，由 packageManager 驱动。
2. **edit `$` 替换模式静默损坏文件**：`String.replace` 字符串形式解释 `$&`/`$$`/`` $` ``/`$'` → 改函数形式 `() => newText` 按字面写入。
3. **bash 超时杀不掉进程树**（Windows 实证：timeout 后工作进程仍完成写文件）→ 改 spawn：Windows `taskkill /T /F` 按父子关系树杀（实测 detached 会使管道输出全空，故 Windows 不用 detached）、POSIX `detached + process.kill(-pid)` 杀进程组（ESRCH 容错）；杀后仍回收输出与退出码；description/注释如实声明守护进程化进程不承诺杀死。

**P2 修复**：取消分类补 `signal?.aborted` 检查（provider 按契约正常结束迭代时半截文本不再冒充 end_turn）；审批/lockKey 回调异常在 executor 内转为该调用 `ok:false`，loop 侧兜底确保 tool/result+step/end 必落盘；grep 双实现对齐（回退补跳过隐藏/.env、formatMatches 仅归一化路径段——顺带修掉 rg `.\path` 前缀与回退输出不一致、匹配文本逐字保留、大小写补测试）；write 成功消息改 `path.relative`（越界回退绝对路径）；glob 排除 node_modules/.git；删除 write/edit 无效 lockKey（types.ts 注明 Ph3 预留）；测试计数口径更正并以最终数字更新；architecture.md 注明不变量边界（ChatRequest.tools 暂不在重建范围，Ph3 评估）。P2-6（reader 容错口径）评估为无害不修，登记 OPEN.md。

**验证证据（全部实跑）**：`pnpm -r typecheck` 两包 Done；`pnpm test` → core **106 passed + 1 skipped** + cli **3 passed** = **109 passed + 1 skipped**（新增 12 个回归测试）；`traj` 双 fixture 渲染正常（exit 0）。

**Release note 素材**：阶段 2 独立审查 3 P1 + 8 P2 闭环；bash 工具获得跨平台进程树击杀能力；grep 双实现输出可逐字节一致。

### 阶段 2 验收（编排者记录）

- 独立审查：pass-with-fixes（3 P1：CI pnpm 版本冲突必现失败、edit $ 替换模式损坏文件、bash 超时杀不掉 Windows 进程树；9 P2，其中 grep 双实现不一致存在敏感文件进入模型上下文的风险）。
- 修复代理：P1×3 + P2×8 修复（commits 93763fa、3902d02），+12 回归测试；P2-6 评估无害留档 OPEN.md。
- 编排者重跑证据：`pnpm -r typecheck` 2 包 Done；`pnpm test` **core 106 + cli 3 = 109 passed + 1 skipped**；loop-demo/demo-session 双 fixture traj 渲染正常；`git ls-files` 0 敏感文件；P1 修复逐一抽查属实（ci.yml 无 version 输入、edit 函数式替换、bash taskkill/进程组树杀）。
- **阶段 2 验收通过**，计划状态改「已完成」。测试累计 33 → 109。
- 遗留标记：CI 真实运行需远程仓库（待用户创建并授权 push）；grep rg 的 .gitignore 行为差异已在 description 声明；POSIX 进程组击杀路径 Windows 机器无法实机验证（注释已声明，待 CI 三平台验证）。

---

## 同日第五段：阶段 3 执行记录（分支 `feat/phase-3-providers-config`）

> 实现代理按 `docs/ai-framework/plans/2026-09-06-phase-3-providers-config.md` 完整执行 Task 1–7。

**交付**：OpenAI-compatible + Anthropic 真实协议 provider（手写 fetch，无 SDK）、两级配置体系（深合并/`${VAR}` 展开/auth 分离）、provider 工厂（`{channelId, model}` 按角色）、审批策略配置化（三 mode + per-tool）、CLI `config check`。

**按 Task 执行记录**：

- Task 1（982cea3）：`config/{redact,schema,auth,load}.ts`——两级加载深合并、`${VAR}` 展开（缺失保留原样+告警）、schema 交叉校验（roles→channel/model）、auth.json 分离存储（chmod 600 尽力）、脱敏工具；21 测试。
- Task 2（2cfb2f8）：`provider/openai.ts` + `test/helpers/sse-stub.ts`——SSE 逐行解析（跨 chunk 缓冲、TextDecoder 流式多字节）、tool_calls 按 index 组装、reasoning_content、usage、401 脱敏、半帧断流（`stream_truncated`）、abort；11 测试。
- Task 3（dce6f10）：`provider/anthropic.ts`——messages API 事件映射（text/thinking/input_json_delta/message_delta/error）、tool_result 合并进 user 消息、x-api-key + anthropic-version 头；9 测试。
- Task 4（0f84465）：`provider/factory.ts`（roles→channel→key→协议分派，ConfigError 脱敏）+ loop 透传 reasoning（session 加性字段）+ runTurn×stub E2E（wire 请求与日志投影重建全等的不变量断言）；5 测试。
- Task 5（485bfb6）：`approval/policy.ts`——default/acceptEdits/bypass 三 mode × 工具矩阵、per-tool 优先、对接 Ph2 executor 审批缝；8 测试。
- Task 6（b54d039）：CLI `config check [--root/--home]`——脱敏展示 providers/roles/key 来源（auth.json/env:XXX/**missing**），错误一行 exit 1；8 测试。
- Task 7：文档整备（architecture.md 配置/Provider 小节、ROADMAP P0-7/8/9、本日志、HANDOFF、OPEN.md 登记真实端点手工验证）。

**执行中发现并修复的问题**：

1. 配置解析失败后不应带着空合并继续校验（出现第二条例行错误）→ 解析错误即致命提前返回。
2. 半帧断流测试首版 socket 直接 RST 会丢弃在途数据（fetch 建连失败而非截断）→ stub destroy 前先让数据到达客户端；provider 把「流式阶段连接中断」从 network 细分为 `stream_truncated`。
3. 工厂测试一例误用真实 `api.anthropic.com` baseUrl 被 403 抓包 → 立即改为本地 stub（提交的代码零真实端点）；该次调用发送的是测试假 key，无真实密钥泄漏。

**契约扩展（经计划许可的最小加性扩展）**：`StreamChunk` 新增 `reasoning-delta` 变体 + `assistant/message` 新增可选 `reasoning` 字段；loop 既有行为与既有块语义不变。

**验证证据（全部实跑）**：`pnpm test` → core **160 passed + 1 skipped** + cli **11 passed** = **171 passed + 1 skipped**；`pnpm -r typecheck` 两包 Done；密钥 grep 自查（见下）零命中。

**测试数**：109 → 171（新增 62：config 21 + providers 25 + policy 8 + cli config check 8）。

**残留**：真实端点（DeepSeek/智谱/Anthropic）实机验证待用户提供 key（OPEN.md 已登记）；CI 三平台真实运行仍待远程仓库。

---

## 同日第六段：阶段 3 独立审查修复（分支 `feat/phase-3-providers-config`）

> 修复代理按独立审查清单执行：P1×2 + P2×7 修，P2-8 评估不修（登记 OPEN.md）。

**P1 修复**：
1. **OpenAI-compatible 中流错误帧被静默吞掉**：`{"error":{...}}` 帧（无 choices）不再被忽略——脱敏后抛 `ProviderError`（error.type 白名单映射 code，未知归 `api_error`，消息含厂商 message）；错误帧+[DONE] 不再变空成功回复，无 [DONE] 不再误报 `stream_truncated`（Insufficient Balance 类真实原因得以透出）。
2. **Anthropic wire 连续 user 消息**（真实 API 必 400）：`toAnthropicWireMessages` 重写为「追加进末条 user 消息的 content 块数组」（tool_result 与 text 块共存），覆盖 turn 以 error/cancel/max_steps 结束后 user 与 tool_result-user 相邻的真实路径；同步修正一版断言了不合法形态的既有测试。

**P2 修复**：error 事件保留原 ProviderError code（不被误包 `stream_truncated`）；终止原因白名单透传（OpenAI length/content_filter、Anthropic max_tokens/refusal，`pause_turn`→`paused`+TurnResult.warning，续跑未实现登记 OPEN.md；新增 `ProviderStopReason` 类型）；tool_calls delta 缺 index 按新 id/新名分槽（不再全并槽 0）；config check 的 baseUrl 过 `redactObject` 脱敏（该函数落地首个生产调用点）；auth.json 损坏错误一次列全全部缺/空 apiKey 渠道；config check 改先校验后输出（不再 "config OK" 后 exit 1 自相矛盾）。

**验证证据（全部实跑）**：`pnpm test` → core **170 passed + 1 skipped** + cli **12 passed** = **182 passed + 1 skipped**（新增 11 个回归测试）；`pnpm -r typecheck` 两包 Done；`config check` 冒烟三分支（无配置单行 error exit 1 / 损坏 auth 列全渠道 / 合法配置完整报告）。

**测试数**：171 → 182（+11）。

**残留**：P2-8（SSE 多行 data 帧）评估不修留档；`pause_turn` 续跑待阶段 4 评估；真实端点手工验证与 CI 远程验证仍待用户（OPEN.md）。

### 阶段 3 验收（编排者记录）

- 独立审查：pass-with-fixes（2 P1：OpenAI 中流错误帧被吞成"空成功回复"、Anthropic 相邻 user 合并缺失必 400；7 P2；密钥安全/真实端点红线专项核查通过——实现期误打真实端点事故确认已清理）。
- 修复代理：P1×2 + P2×6（commits 324c98f、58885f7），+11 回归测试；P2-8（SSE 多行 data 帧）按取舍登记 OPEN.md 不修。
- 编排者重跑证据：typecheck 2 包 Done；`pnpm test` **core 170 + cli 12 = 182 passed + 1 skipped**；P1 修复抽查属实（chunk.error 透出脱敏、user 合并规则）；`config check` 冒烟（无配置单行 error exit 1）；0 敏感文件。
- **阶段 3 验收通过**，计划状态改「已完成」。测试累计 33 → 182。
- 遗留标记：真实端点实机验证（待用户 auth.json 配 key）；CI 待远程；pause_turn 续跑待 Ph4 评估；SSE 多行 data 帧低风险不修（OPEN.md 有影响说明）。

---

## 同日第七段：阶段 4 执行（分支 `feat/phase-4-cli-m1`，M1 v0.1 → CLI 可用）

> 实现代理按 `docs/ai-framework/plans/2026-09-06-phase-4-cli-m1.md` 从 Task 1 执行到 Task 7。

### 执行记录（每 Task 一提交）

- **Task 1 文件快照存储**（fa523fb）：`session/snapshots.ts` —— `SnapshotStore` 挂会话目录（辅助文件 `rewind_points.jsonl`，绝不回改主日志）；`capture`（执行前，仅内存）→ `commitAfter`（成功后整条落盘；失败/取消无条目）；`restore(toSeq)` 按文件取最早 before（创建→删除）、`restoreAfter(fromSeq)` 取最新 after；冲突检测（externallyModified，恢复报告后仍执行）、dryRun、崩溃残行容错、路径规范化。22 测试。
- **Task 2 loop 快照钩子**（916e76c）：`TurnOptions.snapshots`；执行器 `ExecutionEnv` 增 `onBeforeExecute`/`onAfterExecute` 钩子（捕获失败该调用失败——无 before 不允许改文件，undo 完整性优先；提交失败如实转失败结果）；loop 落 tool/call 时记 seq、write/edit 钩子按工具名+file_path 解析目标；取消路径不 commitAfter。含 loop 级「write→undo→文件复原」用例。5 测试。
- **Task 3 会话管理器**（e43ab73）：`session/manager.ts` —— 布局 `~/.harness2/sessions/<encoded-cwd>/<id>/`；encodeCwd 规则（字母/数字/._- 保留、盘符冒号丢弃、`\`与`/`→`--`、其余→`-`：`D:\a\b`→`D--a--b`；>120 字符截断+sha1）；create/list（mtime 倒序+摘要）/search（子串、大小写不敏感）/resume（残行恢复沿用）。14 测试。
- **Task 4 undo/redo 内核**（0287d3f）：`session/undo.ts` —— undo = 最近活动 user/message 前一 seq 追加 `rewind/marker{reason:'undo'}` + restore；redo = 回放 undo 栈取最后未重做项 M，追加 `{rewindToSeq:M.seq-1, reason:'redo'}` + restoreAfter(M.rewindToSeq)；dryRun 无副作用；n 级链/边界（无 user、撤到 0、redo 无 undo）全覆盖。**投影语义最小扩展（向后兼容）**：`computeProjection` 为 reason 前缀 `redo` 的标记增加「按 rewindToSeq+1 精确中立化被重做 undo 标记」——否则纯标记投影只能遮蔽不能复活，redo 无法工作；非 redo 标记行为与阶段 1 完全一致。undo 18 测试 + reader 中立化 3 测试。
- **Task 5 chat REPL**（7ea34cd）：`packages/cli/src/{chat,render,commands}.ts` + index 挂接——readline REPL（提示符 `> `）、流式渲染（text-delta 直写、工具调用/结果单行、turn 摘要行、reasoning 不渲染；core 增最小观察缝 `TurnOptions.onStream`）、命令集（/new /sessions [关键字] /resume /undo [n] [--dry-run] /redo /help /exit、Ctrl+C 取消 turn/双击退出）、审批内联交互（y/a/n；"总是"仅进程内缓存）、`--provider mock` 内置演示（不接配置）。cli 集成测试 4 例（spawn+piped：流式+undo+redo 文件断言 / 会话命令 / 空行 / 审批 ask 路径+缓存+密钥不出现在输出，审批用 127.0.0.1 OpenAI SSE stub）。
- **Task 6 M1 发布物料**（d4a51c2）：cli 包名 `@harness2/cli`→`harness2`（去 private，+files/engines/publishConfig/license；依赖改 `workspace:^`），core 补同款发布字段；根工作区更名 `harness2-monorepo` 避免与 npm 包名重名；CHANGELOG.md v0.1.0（三层能力）；README 重写（30 秒快速开始+bash 快照限制与 Windows 注意如实声明）；`.github/workflows/release.yml`（tag→三平台 build+test→pnpm publish，NPM_TOKEN 缺失跳过并告警；js-yaml 语法校验过）；OPEN.md 登记发布待授权 + M1 手工验收清单。
- **Task 7 整备**：本段日志 + architecture.md（快照/undo/会话布局/chat REPL 小节）+ ROADMAP（P0-10、P1-19 → ✅）+ HANDOFF（M1 状态、chat 冒烟命令、新坑登记）。

### 关键结论/决策（阶段 4）

1. **redo 必须扩展投影语义**：append-only + 纯遮蔽投影下 redo 无法复活事件；采用「redo 标记按 rewindToSeq+1 精确中立化被重做 undo 标记」的最小扩展，旧日志零影响。
2. **undo/redo 全程 append-only**：对话=投影截断（marker 追加），文件=快照恢复（独立文件），session.v1.jsonl 无任何回改。
3. **pause_turn 续跑评估结论：推迟**——续跑需把含 pause 块的原始 content blocks 回传重发，与现有「每 step 从日志投影重建请求」不同构，改动面大于当前收益；现有 `paused` 终止+warning+日志完整已如实安全，用户重新发消息即可无损继续。重启条件：引入 Anthropic server-side tools / 长时任务（Ph7/Ph8 附近）。已登记 OPEN.md。
4. **审批"总是允许"不落盘**（进程内会话级缓存）；mock 演示不接配置不触发审批（零 key 可用）。

### 冒烟记录（`node packages/cli/dist/index.js chat --provider mock`，exit 0）

```
会话: 20260906-001507-13fb66（新建）
harness2 chat — provider: mock（mock 演示：不加载配置）
> 好的，我来创建演示文件。
> write ({"file_path":"harness2-demo.txt","content":"harness2 mock 演示文件\n…"})
< ok [demo-write-1]
已写入 harness2-demo.txt，我再读一遍验证。
> read ({"file_path":"harness2-demo.txt"})
< ok [demo-read-1]
演示完成：已写入并读取 harness2-demo.txt。…
[end_turn · steps 3 · toolCalls 2]
/undo --dry-run → 预览（未执行）：将撤回 4 条消息，rewind 到 seq 1
  - …\harness2-demo.txt → 删除创建的文件（待执行）
/undo → 已撤回 4 条消息（rewind 到 seq 1）；（已执行）删除创建的文件  ← 文件确已删除
/redo → 已重做 4 条消息（rewind 到 seq 15）；恢复内容 "harness2 mock 演示文件…"（已执行）← 文件内容核实恢复
/sessions → 20260906-001507-13fb66  09-06 08:15  4 条 *  开始演示
/exit → exit 0
```

会话目录布局核实：`~/.harness2/sessions/C--Users--hp--AppData--…--tmp.RpeebLMP3O/<id>/session.v1.jsonl`（编码规则生效）。

### Release note 素材（阶段 4 增量）

- M1 v0.1：终端里接真实模型干活——chat REPL（流式渲染/命令集/审批交互/零 key mock 演示）、/undo /redo（投影截断+文件快照，冲突检测+dry-run）、会话管理（全局布局/列表/搜索/恢复）、双协议 Provider、密钥分离与全链路脱敏、248 个自动化测试。

### 测试数与验证证据

- `pnpm test`（build+test）：core **232 passed + 1 skipped**（13 文件）+ cli **16 passed**（3 文件）= **248 passed + 1 skipped**；`pnpm -r typecheck` 两包 Done。
- 测试数：182 → 248（+66：snapshots 22 + loop 快照 5 + manager 14 + undo 18 + reader 中立化 3 + chat 集成 4）。

**残留**：npm 发布/Release 待人类授权（OPEN.md）；真实模型 chat 全流程手工验收待用户配 key（OPEN.md 有清单）；Windows Terminal 实机 REPL 体验待人工；chat 审批交互的 tty 真机行为（Ctrl+C 取消等）仅代码路径覆盖，piped 测试未触发 SIGINT。

## 阶段 4 独立审查修复（P1-1 + P2-2~P2-6）

- **P1-1（假绿）**：release.yml publish 步骤 `continue-on-error: true` → `if: env.NPM_TOKEN != ''` 条件跳过 + 无 token 时独立 notice 步骤输出 `::notice`；"跳过"与"真实发布失败"（token 失效/包名冲突/网络）不再混同——配置后发布失败即红。js-yaml（仓库外 temp）语法复验通过。
- **P2-2（审批等待可取消）**：chat.ts `askUser` 与 turn 取消信号竞速——abort 时 ask 以取消哨兵结束，onAsk 输出「审批等待被取消（该工具调用按拒绝处理）」并返回 false（executor 落 denied），turn 以 cancelled 收尾不再悬挂；取消时清空 answerResolver，取消后的输入行不再被当答案吞掉。新增进程内测试 `chat-cancel.test.ts`（terminal 模式 readline 经 \x03 注入 SIGINT + 本地 SSE stub）。
- **P2-3/P2-4（文案）**：审批提示 `[a]本会话总是（该工具后续所有调用不再询问）`；/help 与 README 各补 redo 遮蔽说明（撤销之后新输入的消息移出当前上下文，仍留日志可用 traj 查看）与 [a] 粒度一句。
- **P2-5（fsync 一致性）**：新建会话删除 `{ fsync: false }`，与恢复路径统一为默认 true。
- **P2-6（覆盖缺口）**：+4 测试——`--session <id>` 恢复（恢复后 /undo 联动文件）、`/resume <id>`（/sessions 标记当前）、`/undo 2`（连续撤两个 turn + /redo 只重做一层）、reader redo 定位落空（rewindToSeq+1 非标记）优雅 no-op；「文件领先于对话」中间态口径留档 architecture.md 快照小节与 OPEN.md（实现不改）。

**测试**：248 passed + 1 skipped → **253 passed + 1 skipped**（core 233+1，cli 20）；`pnpm -r typecheck` 两包 Done；mock chat 冒烟（/undo --dry-run → /undo → /redo → /exit）exit 0。

**残留**：Windows Terminal 实机 REPL 体验与审批取消手感待人工（本轮登记 OPEN.md）；npm 发布/Release 待人类授权（OPEN.md）。

### 阶段 4 验收（编排者记录）——M1 代码部分就绪

- 独立审查：pass-with-fixes。**redo 中立化协议经 8 个探针实证正确**（重点构造的过度复活场景被机制天然排除）；唯一 P1：release.yml 把"无 token 跳过"与"真实发布失败"混入同一 continue-on-error（假绿风险）。P2：审批等待不可取消、"本会话总是"粒度文案、/redo 遮蔽新消息未提示、新建会话 fsync 不对称、覆盖缺口 4 处。
- 修复代理：P1 + P2 全项（commits f6b6a9d、68b498c），+5 回归测试（审批等待可取消、/resume、/undo 2、redo no-op 边界）。
- 编排者重跑证据：typecheck 2 包 Done；`pnpm test` **core 233 + cli 20 = 253 passed + 1 skipped**；release.yml 无 continue-on-error（条件跳过+硬失败）；chat mock 冒烟正常；0 敏感文件。
- **阶段 4 验收通过**。测试累计 33 → 253。
- M1 状态：代码与物料全部就绪；`npm view harness2` 404（包名可用）。发布动作待用户授权（NPM_TOKEN/远程仓库/push tag）。

---

## 同日第八段：阶段 5 执行（分支 `feat/phase-5-server-desktop`，会话服务化 + Electron 桌面壳）

> 实现代理按 `docs/ai-framework/plans/2026-09-06-phase-5-server-desktop.md` 从 Task 1 执行到 Task 6。

### 执行记录（每 Task 一提交）

- **Task 1 服务核心 HTTP 控制面**（7aa1401）：`core/src/server/{sessions,http}.ts`——SessionHub（会话注册表/同会话串行 turn 队列/abort/undo redo 直调内核 + busy 互斥）；EventMirrorWriter 落盘事件镜像；审批缝 onAsk 上抛待处理表（响应/超时 120s/取消均落定）；契约 v1 HTTP API + 端口锁 `~/.harness2/serve.lock`（陈旧接管）+ `/api/config` 脱敏报告（`config/report.ts` 与 config check 同源）；CLI `serve` 命令（stdout 一行 JSON 端口）。加性扩展：loop onStream 增 `reasoning-delta`、manager 增只读 `locate`、writer 增 `SessionAppender` 写入面（镜像 writer 结构兼容，不产生第二写者）。14 测试。
- **Task 2 WS 事件面**（2f67be1）：`server/ws.ts`——单连接多会话订阅，user-message → 串行 runTurn，delta(text/reasoning/tool) 与落盘 event 双通道、turn-end、审批往返、坏帧 error；SessionHub hooks 改多观察者（addHooks 退订）；startServe 内挂 WS。MockProvider 加 reasoningChunks。9 测试（含 delta 与落盘一致性断言、双会话并行、abort cancelled、审批 allow/deny 往返）。
- **Task 3 桌面壳骨架**（f98071f）：`packages/desktop`——ServeManager（spawn `serve --port 0`/stdout 端口 JSON 解析/健康检查/退避重启 1s..15s 封顶+上限 5 次/按锁文件采纳既有实例）；bridge（IPC 命令分发 + WS 连接帧转发）；preload `window.harness2`（sandbox 不允许 require 相对模块 → IPC 通道名内联 + 静态一致性测试）；React+Vite 渲染骨架；`--smoke` 无头冒烟（stdout `{ok,port,rendererLoaded,bridgeReady}`）。12 测试。
- **Task 4 对话 UI**（c25ee56）：`chat-model` 事件流→渲染条目纯投影（turn 标头/气泡/工具行配对/reasoning 折叠/流式光标/turn 摘要/影子过滤）；store 每会话独立 SessionStream（切换不断流核心）+ delta 一致性清空规则 + rev 版本通知；对话视图（Enter 发送/Shift+Enter 换行/停止=abort/审批按钮/未读徽标/自动滚底）。+18 测试（含 4 个 jsdom 组件测试）。
- **Task 5 分屏拖拽**（8ec832c）：`shared/layout.ts` 布局引擎（1..3 栏/分配去重/normalize 唯一校验口）；持久化 `~/.harness2/desktop-layout.json`（主进程容错读写）；HTML5 DnD（dataTransfer 优先 + dragState 回退供 jsdom）；后台会话徽标（未绑定分栏的已订阅会话）。+7 测试。
- **Task 6 win 打包与整备**（本轮提交）：electron-builder win nsis（unsigned，publish null），产物落 `release/`；cli 以 esbuild 单文件 bundle 进 extraResources（`--alias:jsonc-parser=lib/esm/main.js`——UMD 运行时动态 require 打不进单文件）；CI 加 desktop 构建任务（windows：build+单测+dist+上传 nsis 产物）；architecture/ROADMAP（P1-11/12/13 ✅）/HANDOFF/本日志/OPEN 整备。

### 执行中发现并修复的问题

1. 端口锁写入前未创建 `~/.harness2` 链（ENOENT）→ acquireServeLock 补 mkdir。
2. sandbox preload `require('../shared/protocol.js')` 失败（sandbox 白名单只允许 electron/events/timers/url）→ IPC 通道名内联 + 静态一致性测试。
3. 冒烟首版 renderer 路径错误仍报 ok（did-finish-load 对加载失败也触发）→ 修路径并加强为 executeJavaScript 校验 `window.harness2`（bridgeReady）。
4. 冒烟退出后进程悬挂：serve 子进程持 stdout 管道 → finish 前先 stop serve；被杀残留进程持 serve.lock 曾导致后续 serve 退出码 1（清理后增设 smoke 临时 home 隔离真实 ~/.harness2）。
5. `useSyncExternalStore` 不触发渲染：流缓冲在 AppState 外的 Map 里，state 引用未变 → notify 时 bump rev 版本号。
6. App 渲染崩溃（SessionList 中 `toLocaleString` 漏括号，函数作 React 子）→ 修复；顺带把 `applyLayout` 改走 normalizeLayout（loadLayout 返回 undefined 曾致 `state.layout.panes` 崩）。
7. 重启计数"成功即清零"会让必崩服务无限重启 → 改连续计数（stop 后 start 才归零）。
8. electron-builder 26.15.3 与 pnpm 解析的 @electron/get 3.0.0 不兼容（缺 ElectronDownloadCacheMode）→ workspace overrides 钉 `@electron/get: ^5.1.0`；pnpm 11 的 allowBuilds/overrides 须写在 pnpm-workspace.yaml（package.json 的 pnpm 字段已被忽略）。
9. esbuild bundle 后运行报 `Cannot find module './impl/format'`：jsonc-parser UMD 运行时动态 require 逃过打包 → bundle 脚本 alias 到其 ESM 入口。

### 冒烟与打包记录（全部实跑）

- CLI serve 冒烟：`{"port":63034,"pid":34036}` → /api/config/sessions/events/undo dryRun/锁文件全验证通过。
- 开发态桌面冒烟（多轮复跑）：`{"ok":true,"rendererLoaded":true,"bridgeReady":true,"port":52680/53652/65060}` exit 0。
- **打包产物**：`packages/desktop/release/harness2 Setup 0.1.0.exe`（112,040,360 字节 ≈ 106.9 MiB，win nsis x64 unsigned）+ `release/win-unpacked/`（376 MB，含 `resources/cli/harness2-cli.cjs` 608KB 单文件 bundle）。
- **打包后冒烟**：`release/win-unpacked/harness2.exe --smoke` → `{"ok":true,"rendererLoaded":true,"bridgeReady":true,"port":64732}` exit 0（spawn bundle serve + preload 桥全链验证）。
- nsis 安装/卸载/启动全流程待真机（OPEN.md 登记）。

### 测试数与验证证据（交卷前复跑）

- `pnpm test`（build+test）：core **256 passed + 1 skipped** + cli **20 passed** + desktop **37 passed** = **313 项（312 passed + 1 skipped）**；`pnpm -r typecheck` 三包 Done。
- 测试数：253+1 → 313 项（+60：server 14 + ws 9 + desktop 37）。

**残留**：GUI 真机项（窗口交互/拖拽手感/实机多会话体验/Windows Terminal）、nsis 安装卸载流程、真实 provider 端到端（待用户 key）——全部登记 OPEN.md。
### 阶段 5 验收（编排者代审）——桌面端第一版就绪

- **基础设施事故**：独立审查代理连续 4 次失败（captcha verify failed / Model request failed×3，最短 1.9s 即拒），按预案降级为编排者代审，OPEN.md 登记补审待办。
- **命门核查**：`EventMirrorWriter`（实现报告误写作 SessionAppender）= 真实 SessionWriter 的观察包裹，append 委托持锁 writer 后镜像回调——单写者不变量成立，CLI 占锁正确映射 locked。P0/P1 无。
- **代审发现**：5 个 P2 全部留档 OPEN.md（WS 帧上限/本地信任域假设/锁时序/采纳 root 一致性/竞争窗口），暂不修。
- **重跑证据**：typecheck 3 包 Done；`pnpm test` = core 256+1 + cli 20 + desktop 37 = **312 passed + 1 skipped**；serve 冒烟（/api/config 脱敏 ok:false、/api/sessions）；CSP connect-src 'none'；0 敏感文件。
- **阶段 5 验收通过**。测试累计 33 → 312+1。桌面安装包 `harness2 Setup 0.1.0.exe`（106.9MiB）已产出。
- 遗留：GUI 真机清单、nsis 安装流程、桌面真实 provider——待用户实机与 key。

## 阶段 6 实现记录（feat/phase-6-memory-fork：记忆系统三态 + 会话分叉）

> 交接提示词驱动的完整实现（阶段计划 `docs/ai-framework/plans/2026-09-06-phase-6-memory-fork.md`）；基线 312 passed + 1 skipped。

### Task 提交链（每 Task 一提交，gitmoji 中文）

- **Task 1 system 缝 + memory/snapshot 事件**（ce09604）：`ChatRequest.system?` 加性可选（openai → 首条 system 消息 / anthropic → 顶层 system 参数）；事件类型清单加 `memory/snapshot`（payload `{content}` 非空字符串，解析层 + writer 写入口双重校验）；渲染不特殊处理（通用兜底行）。测试：两协议 wire 映射（有/无 system）、payload 校验、writer 往返、旧日志兼容。+8 测试。
- **Task 2 记忆存储与工具**（2571696）：`memory/store.ts`（MEMORY.md/USER.md、`\n§\n` 条目、预算 2200/1375 字符**最终态**校验、同进程 promise 链互斥 + tmp+rename 原子写、漂移检测拒写 + .bak 备份、注入扫描警告仍写入）+ `memory/tool.ts`（add/replace/remove 单操作或 operations 批量原子，MemorySink 缝）。+24 测试（round-trip/预算/删旧加新/原子批量/漂移/注入扫描/200 并发不丢更新/工具形态）。
- **Task 3 记忆开关与冻结注入**（bad60b4）：config `memory` 段（off|ask|auto + nudgeInterval，缺省 off 尊重隐私）；loop `TurnOptions.memory`——活动投影已有 `memory/snapshot` → 复用 content（**会话内冻结**不重读文件）；没有 → 读 store 组装、先落事件再注入 system（空记忆不注入；老会话首个新 turn 补快照；漂移按空记忆处理）。**不变量扩展：`mock.requests[].system` 可从日志逐步重建**。+8 测试 + config 1 测试。
- **Task 4 nudge 后台复盘 + pending 审批**（2dc7690）：`memory/pending.ts`（pending/<ts>-<id>.json 暂存，approve 重放执行——预算/漂移校验照常生效、失败保留暂存**只延迟不丢弃**，reject 显式丢弃）；`memory/nudge.ts`（复盘 turn = roles.small provider + 独立系统提示经 memory/snapshot 冻结进一次性临时会话 + 只挂 memory 工具 + 对话摘要确定性截断，跑完即删，主日志零污染）；SessionHub nudge 计数（turn 内调过 memory 工具归零）+ fire-and-forget 复盘（turn-end 后异步，主对话零阻塞，异常收口 nudge-finished.error）；WS 加 `nudge-started/finished` 提示帧；hub 按 mode **按会话换装** memory 工具（ask → 暂存带来源会话归因，auto → 直写）；CLI `harness2 memory show/clear/pending/approve/reject` + chat REPL 按 mode 注册工具与注入 store。+20 测试。
- **Task 5 会话分叉**（6da111e）：`session/fork.ts` `forkSession(manager, id, {atSeq?})`——活动投影截取（非 header/非 rewind/无影子）重放进新会话（新 seq，payload 原样）+ header 血缘 `parentSession`/`isSeeded`；**原会话字节级零变化**；rewind/影子/文件快照不复制（新会话 undo 从零，README 注明）；半成品失败即清理。三端入口：REPL `/fork [seq]`、`chat --fork <id> [--at <seq>]`、`POST /api/sessions/:id/fork` + WS op `fork`（forked 帧）。+10 测试（core 8 + cli 2）。
- **Task 6 整备**（本轮提交）：architecture（记忆/分叉小节 + 事件类型/不变量边界/WS 帧更新）、ROADMAP（P1-14/15 ✅）、HANDOFF（状态快照/记忆分叉冒烟/已知坑）、README（记忆与分叉用户指南 + 分叉 undo 语义注明）、本日志、OPEN.md（真实模型记忆三态验证清单待 key）。

### 实现中修复的问题

1. `MemoryReadResult` 误继承 `MemoryFileUsage`（entries 字段类型冲突）→ 独立接口 + entriesCount。
2. store mutex 泛型对 Promise 返回值的推断错误 → 收窄为同步闭包 + `T | PromiseLike<T>`。
3. harness2 providers.test.ts 的 HarnessConfig 字面量缺新必填 `memory` 字段（typecheck）→ 补 `memory: {mode:'off',...}`。
4. CLI `memory --home` 声明在父命令导致子命令解析不到 → 每个子命令各自声明。
5. CLI `memory approve` 忘绑 store（「未配置记忆 store」）→ 补 MemoryStore 构造。
6. hub reviewRunning 初版用 Map<sessionId> 会覆盖同会话连续复盘的取消源 → 改 Set<AbortController>。
7. CLI fork 集成测试的 Dirent 目录枚举在 worker 环境行为异常（walk 同代码能列到、helper 返回空）→ 改用 banner 提取分叉 id + `manager.locate` + `loadSession` 断言（更直接也更稳）。
8. architecture.md 编辑误吞「桌面端」标题 → 立即恢复。

### 验证证据（交卷前复跑）

- `pnpm -r typecheck`：core/desktop/cli 三包 Done。
- `pnpm test`（build+test）：core **314 passed + 1 skipped** + cli **28 passed** + desktop **37 passed** = **379 项（378 passed + 1 skipped）**（累计 33 → 379）。
- 红线自查：记忆内容零入 git（测试全临时目录）；off 模式零 store 写入（loop 不传 memory + 装配层不建 store）；契约扩展仅两处（ChatRequest.system、memory/snapshot 事件）。

**残留**：真实模型记忆三态行为待 key 登记 OPEN.md；HTTP memory 管理端点与桌面 fork/memory UI 属后续阶段；跨进程记忆文件锁未做（与阶段 2 会话锁 P2-3 同口径，已在 store.ts 如实声明）。

### 阶段 6 独立审查修复轮（同日追加）

- **修复**：P1×2——serve 入口（sessions.ts runOne）与 nudge 复盘（nudge.ts runNudgeReview）各自补传 `memory` 选项：此前 serve/desktop 主会话零 memory/snapshot（记忆"只写不读"）、复盘预置的 NUDGE_REVIEW_SYSTEM 快照从不生效；P2×5——hub ask 缺 pending 构造期 fail-fast、nudge staged 改 diff 统计（只计本次复盘新增）、pending 上限 200 + `memory pending --clear`、memory clear 漂移文案如实化（无 .bak 不谎称）、注入扫描模式扩充（ignore the above/无视之前指令/忽略上述/泄露你的系统指令等）。
- **补测**：审查指出的覆盖缺口 ×2——rewind 到快照 seq 之前 → 下轮读当前 store 补落新快照；fork 快照两分支（atSeq 在快照后复用冻结内容 / 在快照前补新快照）。新增测试 10 例（core 9 + cli 1）。
- **验证**：`pnpm test` = core 324+1 skipped + cli 29 + desktop 37 = **390 项（389 passed + 1 skipped）**；typecheck 三包 Done；mock chat 冒烟 exit 0。OPEN.md 登记 pending 上限 200 新口径；HANDOFF 测试数字同步。

### 阶段 5 补复审修复轮（同日追加，收尾代理完成）

- **修复**：P1——serve-manager 重启语义：启动期退出（未打印端口行）计入退避链自动重试（首次 start() reject 保留给调用方）、重启链 spawn 失败不再被静默吞掉而是调度下一次退避（达上限才 offline）、restartCount 只在新一轮 start()/stop() 归零。P2×6——①sessionId 格式校验（hub 出口 `/^\d{8}-\d{6}-[0-9a-f]{6,}$/`，`../x` 类穿越原语 400 不触达文件系统）；②hub.close() 进入即清排队消息；③runOne 非预期异常 emitTurnEnd('error') 脱敏收口；④HTTP 健壮性三件（超限停读断连/畸形编码 400/缺 content-type 400）；⑤stdoutBuf settled 后停止累积（settled 前留尾部 8KB）；⑥runSmoke smokeHome 提前声明防 TDZ。
- **收尾说明**：实现大部分由前一代理完成；收尾轮修复 fork.test.ts「未知会话 404」断言按新语义改口径（`nope` 类旧假 id 现被格式校验 400 拒绝，改用合法格式 id），P2-4 三例拆独立测试，OPEN.md 补两条不修登记（WS 增量 rewind 影子、本地信任域加固列 M2 发布前），日志 §13。
- **验证**：`pnpm -r typecheck` 三包 Done；`pnpm test` = core 329+1 skipped + cli 29 + desktop 39 = **398 项（397 passed + 1 skipped）**；mock chat 冒烟 exit 0（write/read/undo/redo/sessions 全链）。HANDOFF 测试数字同步。

### 阶段 5 复审 + 阶段 6 双验收（编排者记录）

- **Ph5 补独立复审**（子代理恢复后补做）：确认单写者结论；**部分推翻代审**——5 个 P2 全部成立（2 条加强：HTTP 无 Origin 校验的跨站打点面、采纳实例死后的永久 connected）+ 新 P1（serve-manager 重启语义：启动期退出不重试、restartCount 永不归零）+ 7 P2。
- **Ph6 独立审查**：pass-with-fixes——2 P1 均为装配漏传（serve 入口与 nudge 复盘都没把 memory 传给 runTurn：记忆"只写不读"、复盘裸跑），测试全绿恰因测试也没覆盖这两个入口；契约外变更为零。
- **修复**：Ph6（`0ecccdc`，+11 例）；Ph5（`c304149`，修复代理被并发限制打断于交卷前——提交已完成，接手代理确认无需返工）。
- **编排者重跑证据**：typecheck 3 包 Done；`pnpm test` = desktop 39 + core 329+1 + cli 29 = **397 passed + 1 skipped**；P1 修复抽查属实（restartCount 归零、启动期退出进重试链、sessionId 格式校验、close 清队列、turn-end 兜底）；chat mock 冒烟 exit 0。
- **阶段 5（复审修订）与阶段 6 均验收通过**。测试累计 33 → 397。
- 教训留档：装配层漏传是测试盲区的典型形态——今后每个新入口（hub/serve/桌面）必须有一条"与 CLI 同语义"的注入断言。

### 阶段 7 执行记录（M2：浏览器/压缩/定时任务）

- 实现代理完成 Task 1–3 并提交（`bd88d23` 压缩 / `f6246c2` 浏览器工具 / `35771f5` 定时任务），Task 4 半成品 + 两次并发限制中断。
- **降级预案第二次启用**：Task 4–6 由编排者代做（留档待补独立审查）。Task 4 审查半成品质量良好，修复其中测试 bug（`getWithHost` 缺 `req.end()` 导致挂起——恰好是中断代理没来得及修的）；Task 5 发布物料（CHANGELOG v0.3.0、README 浏览器/cron 章节、版本号 0.3.0）；Task 6 整备（architecture 四节、ROADMAP #16–18 ✅ + M2 标注、HANDOFF、OPEN）。
- **验证**：`pnpm test` = core 377+1 + cli 29 + desktop 39 = **405 passed + 1 skipped**；typecheck 3 包 Done；chat mock 冒烟 exit 0。
- 教训再留档：今日子代理基础设施三次并发限制中断（Ph7×2、Ph5修复×1），均为长任务；降级-接手-验证的预案已跑通两轮。
- **M2（v0.3）代码层面达成**；发布动作待用户授权（OPEN.md）。

### 阶段 7 验收（编排者记录）——M2（v0.3）正式闭环

- 独立审查（重试后成功，验证重试阶梯有效）：pass-with-fixes——1 P1（`cron run` 手工路径 allow-all 绕过审批，与 serve 调度语义分叉）+ 8 P2（要害：压缩摘要"先到先得"丢弃的恰是近端上下文——方向修正为尾部优先；cron tick 锁 TOCTOU——改 O_EXCL 原子创建）。压缩×rewind 语义经读码确认正确（摘要事件参与投影遮蔽，原文永在）。
- 修复代理：P1 + P2×5 修复（`5dc17d7`），+7 测试（含 stash 验证回归有效性）；P2-7/8 登记 OPEN 不修。
- 编排者重跑证据：typecheck 3 包 Done；`pnpm test` = core 383+1 + cli 30 + desktop 39 = **452 passed + 1 skipped**；P1/P2 抽查属实；0 敏感文件。
- **阶段 7 验收通过，M2（v0.3）代码层面正式闭环**。测试累计 33 → 452。
- M2 发布动作待用户授权（OPEN.md：远程仓库/NPM_TOKEN/推 tag，可与 v0.1 合并为一次 v0.3.0 发布）。

### 阶段 8 执行记录（feat/phase-8-plugins-mcp-subagent：插件总线 + MCP + subagent）

- **实现代理单飞完成全部 Task 1–5**（未触发并发限制），每 Task 一提交：
  - `5d99461` ✨feat(core): 插件总线——`plugins/{types,loader,bus}`：manifest 声明式权限（tools/events/cron，未声明=无权限）、allow 装载审批（CLI `plugin enable` 确认后写入 config）、PluginContext 四件（registerTool/on/log/config）全部带 disposer 且卸载逆序展开、单插件失败收口为跳过+告警。config 新增 `plugins` 段（缺省 enabled+空 allow=最小授权）。
  - `64175a1` ✨feat(core): MCP 客户端——官方 `@modelcontextprotocol/sdk`（^1.30.0）；stdio/Streamable HTTP 双传输；`mcp__<server>__<tool>` namespaced（sanitize 后撞名=跳过+告警，本地优先）；断线退避重启（1s/5s/15s 上限 3，稳定 30s 清零计数防 flapping）耗尽后工具全下线；冲突策略 本地>插件>MCP。测试用同一 SDK 起本地内存 server（InMemory 配对/stdio 子进程/stateless HTTP），零外部依赖；CORE_VERSION 抽到 `version.ts` 断 import 环。
  - `730a2ff` ✨feat(core): subagent——`subagent_start/continue` 工具：独立子会话跑完整 runTurn（header 血缘 parentSession/isSeeded/subagent），**零新增事件类型**（output JSON 带 childSessionId）；深度红线=子工具集减 subagent 工具再按 (depth+1)<maxDepth 重挂（血缘重绑，默认 1）；父 abort→子 abort 且子事件照常落盘；审批上抛同缝（payload.sessionId=子会话）。config 新增 `subagent` 段。
  - `2780389` ✨feat(core,cli,desktop): 装配与端侧——hub 每会话重绑 subagent 工具 + 插件事件桥接；serve 装配链=本地→插件→MCP（config 派生，注入语义与既往一致）；CLI `plugin list/enable/disable`、`mcp list`（连接探测）、chat mock 注入脚本旗标、REPL 会话切换重绑；桌面工具行来源前缀（[MCP:x]/[子会话]）+「子会话 ↗」跳转。
  - Task 5 文档：architecture 三小节（沙箱边界如实声明）、ROADMAP P2-20/24/25 ✅、HANDOFF、本日志、OPEN。
- **关键决策**：①MCP 服务器名在 config 层强制 ^[a-z0-9_]+$（拼进工具名必须过工具名约束）；②重启计数"稳定 30s 才清零"——防"连上即断"flapping 无限循环（纯成功即清零会让 crash-loop 永不进入 down）；③stateless Streamable HTTP 的官方模式是每请求新建 transport（夹具复用传输会 500）。
- **验证**：`pnpm test` = core 434+1 skipped + cli 37 + desktop 41 = **512 passed + 1 skipped**（新增 61 例）；`pnpm -r typecheck` 三包 Done。期间 tools.test P1-3 超时用例偶发一次（重跑即过，与 loop.test 抖动同类，OPEN 已并案登记）。
- 待用户环境：真实 MCP server（filesystem 等）实测、第三方插件从零装载、桌面子会话跳转手感（OPEN.md 登记清单）。

### 阶段 8 独立审查修复记录（P1×4 + P2×6 + 补测×3）

- **独立审查命中一个真竞态**：审查代理动态复现了「MCP 断开落在 listTools 窗口」时 connectAll catch 与 handleDisconnect 各自调度重启 timer——双重连并发、旧 client 被覆盖永不 close（stdio 僵尸）、预算 2 倍速耗尽。修复口径：`scheduleRestart` 幂等收口为唯一调度入口 + `openConnection` 覆盖前先 close + down 时释放半死连接。
- **三处装配层防御补齐**：①插件抢占 `subagent_start` 工具名曾令 chat 整体崩溃（重名 throw 在 try/catch 外）——CLI 经新增 `PluginBus.revokeTool` 剔除插件版 + 告警，serve 侧换装过滤补一次性告警；②插件卸载后 setTimeout 逃逸的 registerTool/on 现被存活检查静默拒绝（不抛错——逃逸回调里的异常会变 uncaught）；③插件单工具重名从「弃整插件」降级为「跳过该工具 + 告警」，对齐计划「冲突告警不中断」。
- **计划红线兑现**：subagent 子会话此前 undo 只回退日志、文件不还原（runTurn 漏传 snapshots）——start/continue 两处补 `SnapshotStore(childDir)`，「独立快照」措辞恢复进 architecture.md；continue 血缘校验补 `subagent === true` 标志 + SESSION_ID_PATTERN（从 manager.ts 导出为唯一事实源，hub 同源引用）。
- **口径修正**：`subagent.maxTurns` 上限 10 与缺省/契约示例 25 自相矛盾——与 maxDepth 分开校验（1..200）。connectAll 改 Promise.allSettled 并行 + 握手超时独立可配（connectTimeoutMs 缺省 5s）；down 状态可经 connectAll 二次调用恢复；`mcp list` 失败如实显示 down 与重试提示。`mutatePluginsAllow` 改 temp+rename 原子写；CLI 退出路径 `mcpManager.close()` 改 await。
- **教训**：审查发现的「插件重名弃整插件」与计划「冲突告警不中断」直接矛盾——实现时未回头对齐计划约束表，靠独立审查兜住；今后每条 Global Constraints 落地时应在测试名里引用对应条目。
- **验证**：`pnpm test` = core 445+1 skipped + cli 40 + desktop 41 = **526 passed + 1 skipped**（新增 14 例：core 11 + cli 3）；`pnpm -r typecheck` 三包 Done；chat mock 冒烟 exit 0。CLI/serve 子会话工具集差异登记 OPEN 留档。

### 阶段 8 验收（编排者记录）——扩展生态三扇门

- 独立审查：pass-with-fixes，4 P1 全部带**动态复现**（MCP scheduleRestart 竞态实测双 timer 同毫秒起跑+stdio 僵尸；maxTurns 校验与契约示例矛盾实测解析失败；插件抢 subagent 名实测 chat 整体崩溃；subagent 子会话零快照违背红线）+ 6 P2。权限绕过面核心项（on('*') 拒绝/config 深冻结/cron 预留声明无绕过）经查无问题。
- 修复代理：P1×4 + P2×6 全修（`5e9f048`）+ 留档（`c869ca9`）；修复代理被并发限制打断于交卷前——**工作已全部提交**，验收照常进行（中断接手流程第三次生效）。
- 编排者重跑证据：typecheck 3 包 Done；`pnpm test` = core 445+1 + cli 40 + desktop 41 = **527 项（526 passed + 1 skipped）**；P1 抽查属实（幂等闸门/契约示例可解析/权威版优先/子会话快照）；0 敏感文件。
- **阶段 8 验收通过**。测试累计 33 → 526。下一站 Ph9（QQ/飞书网关——用户主用 QQ，QQ 开放平台已注册）。

### 阶段 9 审查 fail → 编排者代修（并发限制降级第 4 次）

- 独立审查 **verdict: fail**（项目首例）：P0 startGateway 从不调 adapter.start()（网关对 QQ 完全聋哑）+ 7 P1（重连不重订阅/respondApproval 同步抛/msg_seq 固定 1 撞去重/飞书无策略/webhook 无鉴权/CLI 只接 QQ/测试缺口）+ 8 P2；官方文档纠正三处契约（msg_seq 递增、expires_in 字符串、DIRECT_MESSAGE intent）。
- 修复代理 4 次被并发限制打断（21s~5min），按预案**编排者代修**：P0 + P1×7 + P2×8 全修（`fb837bb`），gateway 改动态加载（修复 chat 演示时序抖动）。全量 540 passed + 1 skipped、typecheck 4 包 Done。
- 教训（第三次同形态）：测试盲区与实现盲区重合——网关生命周期零测试没拦住 P0。后续阶段计划加"新入口必须有生命周期测试"条目。

### 阶段 9 审查修复与验收记录（编排者）

- 独立审查 **verdict: fail**（首份 fail）：P0-1 startGateway 从不调 adapter.start()（网关聋哑——生命周期起点遗漏，测试盲区同位）+ 7 P1（重连不重订阅/respondApproval 同步抛/msg_seq 恒 1 撞 QQ 去重/飞书零策略/webhook 无鉴权/CLI 只接 QQ/测试缺口）。审查引用官方文档纠正 msg_seq 递增与 expires_in 字符串契约——外部规范锚定价值再次验证。
- 修复代理三次并发中断 → 编排者代修（`fb837bb`）：P0 + 7 P1 + 5 P2 全部落地（生命周期/重连重发订阅/审批不抛/msg_seq 按 msg_id 递增/飞书策略闸门+token 校验+reply API/CLI 飞书分支+--platform+错误透出/token 字符串兼容/op9/429 指数退避/路由原子写）。
- 附带发现并修复：gateway 静态导入拖慢 chat 启动（改动态 import，根治 chat 演示用例时序抖动）。
- **全量复验**：540 passed + 1 skipped；typecheck 4 包 Done。
- 教训（第三次同型）：新入口（gateway 生命周期）必须有 start/stop 断言；fail 阶段复审已登记 OPEN（基础设施恢复后补）。
- ROADMAP P2-21 措辞同步修正（删除「全实现」过度声明）。

### 阶段 10 实现记录（feat/phase-10-export-skills：轨迹导出/回放 + Skills → M3 v0.6 代码闭环）

- **Task 1 轨迹导出与回放**（`session/export.ts` + CLI `export`/`replay`）：exportSession 只读白名单打包（session.v1.jsonl 必含；rewind_points.jsonl/snapshots/ 存在即含；lock 永不入包）+ 全库扫描 `header.parentSession` 匹配的**直接子会话**递归入 `subagents/<id>/`（孙会话不在冻结结构内——maxDepth>1 场景需分层导出，已留档）；fflate 条目固定 mtime（2000-01-01，DOS 时间 1980-2099 区间——初版用 `new Date(0)` 当场踩坑 8 例红）+ 排序实现**同内容同字节幂等**；importReplay 逐行 parseEventLine（坏行计数 + loadSession 同款告警）→ computeProjection 摘要，空包抛错。测试 11 例（只读红线逐字节+mtime、幂等、黄金断言、坏行容错、空包拒绝、fixtures/demo-session 一致性）+ CLI 集成 5 例。
- **Task 2 Skills**（`skills/{store,tool}.ts` + loop 注入 + CLI `skill list` + chat/serve 接线）：两级目录（项目 `.harness2/skills/` > 全局 `~/.harness2/skills/`，同名覆盖+告警）、frontmatter 简表（name/description 必填、name 禁空白、引号剥离）、上限 50 截断+告警、坏文件跳过+告警、scan/load 现读磁盘（turn 间新增即时可见）。loop 注入：每 turn 重扫，`[Skills 可用]` 名称+描述列表**追加**进 system（memory 快照在前空行分隔；同一 turn 内冻结；空 skills 零注入；扫描告警并入 TurnResult.warning）；全文走 `skill` 工具（safe）现读磁盘。关键决策：**skills 列表不落事件**——唯一事实源是磁盘目录本身（计划 Global Constraints 明确零新增事件类型），与 memory/snapshot 冻结语义有意区分，architecture.md 已写明口径。测试 15 例 + CLI 3 例。
- **Task 3 M3 发布物料**：CHANGELOG 0.6.0（轨迹导出回放/Skills + 阶段 8/9 摘要 + M1/M2 累积概述）；README 补阶段 8-9 与阶段 10 章节（顺带修正「gateway 占位」过期描述）；四包版本 0.3.0 → 0.6.0（CLI `--version` 改用 CORE_VERSION，不再硬编码——原值 0.1.0 已过期三个版本）；OPEN 登记 v0.6 发布待授权。
- **Task 4 整备交接**：architecture.md 新增「轨迹导出与回放」「Skills」两节（性能与 P2-4 大日志同口径留档）；ROADMAP P2-23/26 → ✅ + M3 达成标注；HANDOFF 快照更新（574+1、分支、冒烟命令、已知坑 4 条）；OPEN 新增「真实长会话导出体积评估」「skill 真机体验待用户」。
- **基线甄别**：开工基线 `pnpm test` 首跑 cli plugins-mcp 1 例超时（5000ms，Windows 高负载抖动），单文件重跑即过——按交接预案甄别，不当回归处理。收尾阶段 cli chat.test 演示 turn 在全量跑中 2/3 失败：master worktree 对照实验（4 次全绿）+ 3 文件组合实验（3/3 绿）定位为既有竞态（`wait('演示完成')` 命中点早于被断言的 `[end_turn`）被新增测试文件的全量负载拉宽，非功能回归——补一行 `wait('[end_turn')` 加固后 cli 全量 3/3 绿。
- **验证**：`pnpm test` = core 471+1 skipped + cli 48 + desktop 41 + gateway 14 = **575 项（574 passed + 1 skipped）**；`pnpm -r typecheck` 四包通过；CLI 冒烟 `--version`=0.6.0、`skill list` 空列表提示正常。新增测试 34 例（core 26 = export 11 + skills 15；cli 8 = export 5 + skill 3）。
- **残留**：真实长会话导出体积评估、skill 真机体验、QQ/飞书真机联调均待用户环境（见 OPEN.md）；阶段 10 独立审查与 `/accept-phase` 验收待编排者流程安排。
