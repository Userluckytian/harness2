# 2026-09-07

### 阶段 10 验收记录（编排者）

- **流程闭环**：实现代理单飞完成 Task 1-4（5 提交，无中断）→ 独立只读审查 **verdict: pass（0 P0 / 0 P1 / 7 P2）**→ P2 处置派回原实现代理（4 修 3 档，2 提交）→ 编排者终验。全程无基础设施中断（Ph5 以来首次全程单飞无降级）。
- **审查 P2 修复**（`e607435`）：导出条目按 rel 排序（跨环境字节幂等）、SkillStore 层内重名单独告警（不再误用跨级覆盖文案）、importReplay 补 loadSession 同款 rewind 越界检查、frontmatter BOM/CRLF 边界补测——均附防回归测试。留档 3 条（子会话 skills 口径并入 OPEN 工具集差异行、importReplay 解压上限、非库布局退化扫描说明）。
- **红线核查**（审查代理实跑 + 编排者抽验）：`git diff master..HEAD -- packages/core/src/session/types.ts` 为空（零新增事件类型）；export 只读由三代会话目录 mtime+逐字节断言钉死；新增代码零密钥路径。
- **编排者终验复跑**：`pnpm test && pnpm -r typecheck` **exit 0**（尾段 cli 48 passed、四包 typecheck Done；全量计数与实现代理交卷一致 = **580 项（579 passed + 1 skipped）**，其中新增 39 例 = export 11+5、skills 15+3、P2 防回归 5）。审查代理另抽跑新增 34 例全绿、两包 typecheck 单独验证。
- **阶段 10 验收通过，M3（v0.6）代码闭环**。功能清单 #23 轨迹导出/回放、#26 Skills 落地——ROADMAP **26/26 项全部 ✅**（#24=MCP、#25=Subagent 已于阶段 8 交付）。测试累计 33 → 579+1。
- master 已快进至阶段 9 顶端后承接阶段 10（`feat/phase-10-export-skills` 合并）；推送 GitHub 备份。
- **教训**：①zip 固定 mtime 必须落 DOS 时间合法区间（1980-2099），`new Date(0)` 直接抛错——初版 8 例红当场暴露，幂等从根上做的决策被验证正确；②审查 P2-1 揭示「文档声明（条目排序）与实现不符」类缺陷——文档写了的承诺要么兑现要么删掉，验收时抽查文档-代码一致性有效。

### 阶段 11 实现记录（feat/phase-11-stabilization：稳定化 + 分发，单飞完成）

- **Task 1 性能基线与预算**（`session/bench.ts` + `scripts/bench-session.mjs`，`37475aa`）：确定性合成日志生成器（mulberry32，同 seed 同日志；20% tool + 10 rewind 穿插）+ `runSessionBench` 计时六项操作；`pnpm bench` 可复跑（`H2_BENCH_EVENTS`/`H2_BENCH_SEED` 可调）。10 万事件基线（Windows/Node 22，两次差异 <10%）：loadSession ≈200ms / computeProjection ≈30ms / list ≈245ms / search ≈420ms / export ≈1.4s / importReplay ≈320ms——**全部 <3s，如实记录达标不优化**（测量先于优化，预算表落 architecture.md 作回归锚点）。附带消化 P2-5：importReplay 256MiB 解压上限（前置中央目录声明体积 + 后置实际体积双闸门，`maxDecompressedBytes` 可覆盖，超限友好报错）。
- **Task 2 子会话口径统一**（`fd77cc4`）：统一到 architecture.md 既声明 serve 语义——子会话工具集 = 宿主集 − subagent 工具 − per-session 绑定类（memory/browser_*，新常量 `SUBAGENT_SESSION_BOUND_TOOL_NAMES`），落点收口核心侧 `buildSubagentChildTools`；skills 注入加性（`SubagentOptions.skills` 宿主同款 store，两端装配接线）——消除「继承 skill 工具但盲调」。测试 6 例含两端工具集相等断言与 CLI 形态防回归钉死。
- **Task 3 分发矩阵**（`f599062`）：electron-builder 补 mac dmg（arm64+x64，`identity: null` 显式跳签名）+ linux AppImage；CI `build-desktop` 三平台 matrix + artifact 上传；release.yml 三平台产物附加 GitHub Release。**修复静默损坏三阶段之久的 bundle**：browser 工具进 core 导出图后 playwright-core 的 chromium-bidi require 打不进 cjs 单文件（阶段 7 起无人跑过 bundle，无远程 CI 故未暴露）——`--external:playwright`（缺失走既有「未安装指引」降级）+ `installBrowserRuntime` 对 cjs bundle 的 createRequire 兜底。本地 win nsis 构建通过（`harness2 Setup 0.6.0.exe`，112MB unsigned；本机网络需 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`，CI 直连无碍）；三 workflow YAML js-yaml 校验过；npm 发布物料复核无改动。
- **Task 4 doctor + 崩溃报告 + 演练**（`1e7bbba`）：`harness2 doctor` 六项分节检查（node/config+auth 脱敏来源/目录可写/MCP 列出或 --probe 实连 5s/会话库逐会话完整性/skills 摘要），各项独立 try/catch、出口全过 redactSecrets、exit 0/1；崩溃报告 `~/.harness2/crash/<ISO>.log`（文件名冒号转破折号保 Windows 合法；内容含版本/平台/会话 id 且脱敏；**无遥测零网络发送**，控制台打印手动反馈指引）。演练三例：①writer 半行截断→recoverTruncatedTail→重开追加无损；③合成日志截断恢复后 export→importReplay 往返坏行 0；②CLI serve 真实 spawn 强杀→陈旧端口锁接管重启（桌面 serve-manager 单测声明复用）。doctor 实测真实环境：639 会话全可解析。
- **Task 5 抖动根治 + 整备**（本提交）：三类抖动同根——断言对「机器绝对速度」敏感被并行负载放大：①loop.test 并行波次改「同批结果落盘间隔」判据（并行 gap≈0 / 串行 gap≈200ms，不依赖绝对速度，durationMs>150 排除空跑）；②tools.test P1-3 击杀竞态根因 = 超时点早于子进程 exec，超时 600→1200ms + 副作用延时 2s→3s 拉开窗口；③plugins-mcp spawnSync 受 5s 缺省超时约束，文件级放宽 30s。retry:2 兜底保留（真回归连败仍红）。**连续 3 次全量 `pnpm test` 全绿（607 项 = 606 passed + 1 skipped）**。整备：architecture（性能预算/doctor/分发矩阵/子会话统一口径）、HANDOFF 快照（607 项、分支、坑 3 条）、OPEN 清理（关闭 importReplay 上限/P2-4/口径差异/抖动并案 4 行，新增「三平台 CI 产物待远程验证」「阶段 11 残留手工验收」2 行）。
- **新增测试 27 例**：core bench 6 + subagent 6 + doctor 6 + 崩溃报告 3 + crash-drill 3 + cli doctor 2 + cli serve 演练 1。
- **残留**：mac/linux 构建与 Release 附加待远程 CI；真实大会话 bench 复核、doctor 实机核对、崩溃报告实机演练待用户环境（OPEN.md）。
