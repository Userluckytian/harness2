# 每日日志 2026-09-08

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 关键结论 / 决策 / 风险与待办 / 下一步。

## 今日完成

### 方向变更 → 采用 R2；清理被取代的旧计划

- **用户确认采用 R2 方向**：Grok 终端复刻（T0–T5）+ 功能优先桌面 harness（D0–D6，共享底座 S0–S7）。桌面**不再复刻 CodexMonitor 界面**，改为功能优先（F1–F8 功能闭环，不评视觉相似度）。
- 删除被取代的旧计划/研究 7 份：`phase-interaction-replication.md`（官方阶段 13，方向废）、保守版 cli/desktop interaction、旧 T0–T9 / B0–B9 计划两份、旧「复刻 CodexMonitor」研究两份。
- 更新激进版三份计划（底部提示词与任务/验收同步 R2）：`core-foundation`（S0–S7）、`cli-interaction`（T0–T5，仅依赖 S0–S6）、`desktop-interaction`（D0–D6，功能优先 harness）。
- `OPEN.md` 顶部加「当前主方向 = R2」说明，清理旧残留行。
- commit：`e7c1fce`（R2 计划定稿与旧计划移除）。

### 共享底座 S0–S7 独立验收与合并进 main（阶段 14-激进-共享底座）

- **首轮独立验收**（对照计划验收表实跑）：core test 51 文件 / 741 passed；cli/desktop typecheck 绿。发现 **P0**：S7 四契约（run-config/plan-state/execution-view/change-review）为空壳（`index.ts` 未导出、serve 层零引用、不在 api-surface-baseline、测试纯函数自测）；P1：cancelAck 无 turn 代次、journal 与 session.log 无原子、retry 预算重启清零、plan-state 时间戳锚定、S6 会话级 steer sink 未接、loop.test 并发 flake。
- **修改清单（含判定标准 A–G）**交实现方修复：FixA（S7 接线）、FixB（cancel 代次）、FixC（崩溃对账/retry 预算可读/plan-state seq 锚定）、FixD（会话级 steer sink + loop flake 改功能性并发探针）。
- **复验（全部实跑）**：core test **57 文件 / 789 passed + 1 skipped**；cli/desktop typecheck exit 0；`serve-functional-query.test.ts` 经真实 `startServe` + HTTP 验证 A/B cwd 不串/脱敏/404；新增 6 测试文件均命中。
- **合并与清理**：`git merge --ff-only feat/notion-i1-runtime` → main tip `b5a702d`（65 文件 +12165 行）；worktree/branch 清理，主工作树只剩 `main`。
- 产物：`runtime.v1.jsonl` 运行账本（非第二套对话正文）+ 结构化契约 `submit/resumeSubscription/approval/cancel/task/steer` + `task-coordinator`（子代理可后台并发）+ WS 层 `protocolVersion=2` + S7 桌面功能闭环契约（有效配置/计划/命令执行/变更审查）。
- 配套修复：`b1c2d81`（桌面设置弹窗打开白屏——React Hooks 顺序违规 + 渲染层错误边界）、`b94a2c5`（cli bundle 改用 serve-only 入口修复 ink 冲突，桌面打包 serve 回归）。

## 关键结论

1. 底座 S0–S7 是 R2 唯一冻结契约层，验收通过并合并 main 后，激进-终端（T0–T5）与激进-桌面（D0–D6）才能开工。
2. 首轮验收抓 P0/P1 的价值：S7 空壳如果没在验收阶段暴露，会带着「已验收」的错觉进入下游两轨。
3. 自动化层验收通过；**端到端/真机仍留待用户**（Windows IME/滚动/中断、真实模型断流重试、桌面 F1–F8 工作流）。

## 风险与待办

- `main` 领先远程 25 commit，**默认不 push**（待用户决定；阶段 15 沿用「只提交本地」口径）。
- 阶段 5/6/8 形式验收欠账（当时按档位跳过）与阶段 9 复审欠账未闭环——已列入 2026-09-09 阶段 15 计划（B5-1 / A5）。
- Windows bash 走 cmd.exe、GBK 乱码等适配缺陷与 R2 终端 T2（Windows 四场景能力闸门）相关——已由阶段 15 A1 承接。

## 下一步

- 2026-09-09：阶段 15 质量收口开工（A0 基线 → B1/B2 → A1/A2/A3/A4 → B3/B4 → A5/B5/B6）。
