# 每日日志 2026-09-09

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 关键结论 / 决策 / 风险与待办 / 下一步。本日为阶段 15「质量收口」主执行日。

## 今日完成

### 阶段 15 计划就绪（单人顺序执行）

- 原双轨并行方案（track-a/track-b）合并为**单人顺序执行**：任务编号 A0–A5 / B1–B6 原样保留，同步点全部取消；B2 全量格式化提到任何逻辑改动之前（唯一排他任务）。
- 人类提供 A2 测试环境：本地统一网关 `http://127.0.0.1:40080/v1` · key `sk-unified-local`（本地口令，不入库）· 模型 `big-pickle`（200K 上下文、纯文本）。openai 与 anthropic 两条协议均已实测连通，**A2-1 不得再以「缺 key」登记 ➖**。

### A0 仓库卫生与基线（`7efc0c5`）

- 删根目录 `.tmp-head-check/`（断链 node_modules）并入 `.gitignore`（`.tmp-*/`）；根 `package.json` 版本 `0.1.0`→`1.0.0` 与四包对齐；`git check-ignore -v` 确认 `dist-bundle/`/`release/`/`dist-electron/`/`.tmp-*/` 命中。
- 基线实跑：`pnpm -r typecheck` 4 包全过 8s；`pnpm test`（含 `pnpm -r build`）42s → **core 789 passed + 1 skipped（57 files）· gateway 14 · desktop 158 · cli 71 = 1032 passed + 1 skipped（94 files）**。
- **API 导出面差异核对**：计划/文档写 372，实测 fixture 489（阶段 14 S0–S7 加性增长 440→489），API 快照对新增不判红故全绿——记遗留风险，归 B5 文档欠账处理。

### B1 OPEN.md 拆分（`2a287e7`）

- OPEN.md 只留真实待办（28 条），19 条已关闭/不修/口径登记迁入新建 `DECISIONS.md`（只搬不删，脚本核验原 47 = OPEN 28 + DECISIONS 19 逐字一致）；`.gitignore` 补 `!docs/issue-log/DECISIONS.md` 让 DECISIONS.md 入库。

### B2 lint/format 基建 + CI + 全量格式化（`1756a92`/`aa5a541`/`b507c08`/`9f6db12`）

- 引入 ESLint(flat) + Prettier，新增 `pnpm lint` 并接入 CI（必过步）；首轮只把明显错误（未使用变量/any 泄漏/floating promise）设 error，其余 warn——存量正常代码不误标红。
- **全量格式化独占一个 `🎨style` 提交**（`9f6db12`，242 文件 +11897/-4989）：175 个 ts/tsx 中 174 个 AST 逐节点一致，1 个为 Prettier 改写 `⎇ ` 尾空格为 `⎇{' '}`（React 渲染等价）；`.prettierignore` 排除 `fixtures/**`，格式化后 api-surface 快照未污染（`git diff 376d0bf HEAD -- fixtures/` 为空）。
- `.gitattributes`（`*.md text eol=lf`）消 Windows CRLF 刷屏。
- 格式化后回归：`pnpm -r typecheck` exit 0；`pnpm test` = 1032 passed + 1 skipped 与 A0 基线逐项一致。

### A1 Windows 可用性 P0（`6e9b9d7`/`4258a73`/`bf31606`/`ca34eaa`/`6035d22`）

- A1-1 bash 工具 shell 探测：`config.bash.shell` > Git Bash > cmd 回退；`harness2 doctor` 实测 → `[OK] bash: bash 工具实际使用 D:\Program Files\Git\usr\bin\bash.exe（Git Bash）`。
- A1-2 子进程输出统一 UTF-8 解码：项目 config `bash.shell=cmd.exe` 端到端 → `ver` 输出 GBK 正确解码、无 U+FFFD。
- A1-3 连续工具失败熔断：连续 5 次失败 → `stopReason=tool_failures` 且 finalText 非空（落盘 `assistant/message`）；`maxConsecutiveToolFailures=0` 关闭。
- A1-4 参数缺失返回 schema 片段 + 最小示例；A1-5 校正 browser 未安装提示。
- 证据：`npx vitest run test/windows-bash.test.ts` → 16 passed；`tool-failure-circuit.test.ts` → 12 passed；`tools.test.ts` → 35 passed（含 P1-3 Windows 进程树击杀）。A-6 真机联网检索留待 A2 环境。

### A2 本地网关端到端（真机，3 批）

- **第 1 批核心协议**（`c03eb6d`）：`harness2 config check --home D:/tmp/h2-a2-home --root .` 两渠道（`local-oai` openai 带 `/v1` / `local-ant` anthropic 不带 `/v1`）均 `models: big-pickle`、key 来源 auth.json、**输出无 key 明文**（全文检索 0 命中）；doctor 7 OK / 0 WARN / 0 FAIL；`chat --no-tui` 双协议流式（oai `reasoning_content` / ant `thinking`）+ 工具调用 + usage；失败降级：baseUrl 写错 404、指错端口 6 次退避（≈2/10/30s），均无 key 明文。遗留：network 错误收尾 finalText 为空，记 issue-log。
- **第 2 批 undo/记忆/压缩**（`1fefc11`）：`/undo --dry-run` → `/undo`（文件复原）→ `/redo`（内容回放）→ `/sessions` 搜索 → 审批 y/n/a 三态 → 记忆三态（off 零注入 / ask pending 审批 / auto 直写 + nudge 复盘不阻塞主对话 + 漂移拒写生成 .bak）→ 压缩（contextWindow 临时 4000 触发 `compaction/applied`，验完还原 200000）。
- **第 3 批 MCP 与插件**（`17fd7ce`）：真实 filesystem MCP（14 个工具、`mcp__filesystem__*` 调用与磁盘逐字一致）；从零写 `a2demo` 插件（manifest + index.js）真实装载/审批/事件订阅/工具调用。
- A2-2 云端厂商（DeepSeek/智谱/Anthropic 官方）**无 key 记 ➖**，不得用本地网关或 stub 顶替；桌面端一轮（八项第 6 项）🟡 需真机人工。

### A3 serve 安全加固（`713a1a2`/`71f0058`/`0900689`/`ba39f9f`，⚠️ 有条件通过）

- A3-1 Origin/Host 白名单 + 一次性 token（`x-harness2-token` / `Authorization: Bearer`，token 只写 `0600` serve.lock 不入日志）；A3-2 WS 帧上限对齐 HTTP 1 MiB 超限断连记账；A3-3 playwright 移入 optionalDependencies + 动态 import（bundle 侧 `--external:playwright` 保留降级）。
- 实测：`serve --port 0 --provider mock` → `{"port":61196,"pid":36928}`；错 token `401`、跨站 Origin `403`、严格模式 `HARNESS2_SERVE_REQUIRE_TOKEN=1` 无 token `401`；desktop smoke（158 passed）实测可连不被误挡。`serve-security.test.ts` → 15 passed。
- **如实偏离/审查结论**：默认「兼容回退」而非强制 token——P1-1（无 Origin 本机进程仍可连 WS）与 P1-2（严格模式 desktop 健康检查把 401 当已连接）登记为**发布前（B6/联合验收）必闭环**，不得写「安全基线达成 ✅」。

### A4 sessions.ts 拆分（`2645c32`→`b3368bb`，7 提交）

- 73KB `sessions.ts` 按 hub 装配/订阅恢复/任务协调三块切分为 8 个文件，`sessions.ts` 71862B→**2844B**（<25KB），其余各 <25KB；纯搬运（71 个成员区块逐字校验，仅 `private→protected` 与 `abstract` 缝）；拆分前后同组测试逐项一致（core 832 passed + 1 skipped）；导出面未变（api-surface 7 passed）。

### B3 cli/desktop 大文件拆分（`6846e6f`/`858fae8`）

- cli `index.ts` 38558B→2104B（13 个命令块逐字搬入 `commands/`）；desktop `App.tsx` 30401B→10228B（app-shared + SessionList/ChatView/PaneArea）；SettingsDialog（30KB）评估后不拆（单一职责+单一消费者，理由记 issue-log）。
- 拆分前后测试一致：cli 71 passed、desktop 158 passed；typecheck 4 包 exit 0；cli 13 个命令手工走查全通（`--help` 14 子命令、`--version` 1.0.0、doctor/config check/chat mock/memory/skill/cron/plugin/mcp/gateway/serve/traj/export/replay）。

### B4 规范文档补齐（`6573261`/`7ee2547`/`58733e9`）

- B4-1 `coding-standards.md`「项目专属约定」全节占位填实（写进去的每条命令都实测过，cli 包名实测为 `harness2`）。
- B4-2 `CODE_REVIEW.md` 增补 harness2 专属红线 8 条（四条不变量/密钥脱敏/快照范围/审批不得弱化/导出面快照同步/插件边界）+ 修「缓存在的使用」错字。
- B4-3 插件边界如实声明：README / 文档站 / `plugin list` 三处「v1 插件与主进程同进程运行（非隔离），manifest 权限仅为 API 层约束，不提供沙箱」。

### A5 阶段 9（IM 网关）独立复审（`1260e32`，⚠️ 有条件通过）

- 复跑 `pnpm --filter @harness2/gateway test` → **5 files / 14 passed**（3.8s，与基线逐项一致，非 flaky）；源码走查 + 7 个临时观察用例运行时实证三关注点：startGateway 重复启动幂等（readyPromise 单飞）、异常退出释放（closed+clearTimeout+ws.close）、断线重连重订阅（close 后 1s 重连 + 全量重发 subscribed）、msg_seq 按 msg_id 递增（同 msg_id 4 条 seq=[1,2,3,4]）。
- 3 个 P1 下放（feishu 端口冲突挂死 / 并发建双会话 / 原「测试缺口」未闭环）——登记 acceptance 第 6 节，须写进下阶段「上阶段遗留」。四段结论落盘 A-11。

### B5 文档与验收欠账（文档部分，本日收尾）

- **B5-1 形式验收补记**：阶段 5/6/8 三份计划文档补「形式验收（/accept-phase，2026-09-09 补）」四段结论——诚实口径写明「本阶段列为『未执行 ➖（档位跳过）』，本次补形式验收记录，不改变既有状态」「形式验收通过 ≠ 完整验证，不构成已验证 ✅ 通过声明」；阶段 9 状态行「复审待基础设施恢复后补做」→「复审已补：有条件通过（A5，2026-09-09）」并把 A5 四段结论归位顶部状态块。
- **B5-3 HANDOFF 快照**更新到当前（阶段 1–15 状态、1032 passed + 1 skipped、四包 1.0.0、阶段 15 计划四件套索引）。
- **B5-4 diary** 补 2026-09-08 / 2026-09-09（本文件）。
- README 三张截图**另处理**（本任务不生成/不占位图片，截图区保持占位注释「图待补」）。

## 关键结论

1. **A2-1 完成本地网关双协议真机验证**——产品最核心路径（真实模型端到端）首次实机打通；云端厂商差异仍是唯一待补（缺 key）。
2. **全阶段测试只升不降**：基线 1032 → A1 后 core 832 + 1（+43 例新增测试）→ 全量仍 1032 passed + 1 skipped / 94 files。
3. 安全（A3）与复审（A5）都按「有条件通过」诚实下放 P1，未冒充全绿；发布前（B6/联合验收）P1 是硬闸门。
4. 单人顺序执行的排他项（B2 全量格式化）在最前面做完，之后所有业务 diff 干净可审。

## 风险与待办

- 发布动作（B6）与 push 全部待人类授权；本阶段**只提交本地**。
- A3 P1-1/P1-2（serve token 默认不强制的两半互斥）为发布前必闭环；A5 3 个 P1 进下阶段「上阶段遗留」。
- A2 桌面端真机一轮、A2-2 云端 key、A-6 Windows 真机联网检索仍待补。
- README 三图待外部补图（B-13）。

## 下一步

- 阶段 15 收尾：B6 发布准备（等人类逐项授权）；然后 R2 激进-终端 T0–T5 / 激进-桌面 D0–D6 开工。
