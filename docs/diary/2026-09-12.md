# 每日日志 2026-09-12

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 遇到的问题与解法 / 验证情况 / 关键结论 / 风险与待办 / 下一步。
> **本篇为 2026-09-17 阶段 V0「基线补账」依 `git log` + `docs/issue-log/2026-09-12.md` / `2026-09-12-D.md` 反推补记，非当日实时撰写**；查不到的细节一律写「无记录」。

## 今日完成

当日提交 56 个（含 00:03–00:36 的凌晨跨日尾巴）：**清两轨审查债 → I1 两轨合入 main → CI 拆包消「单点红掩盖」→ 冒烟手册 → 用户实测 5 项反馈落地（P-1）→ P0 渲染层选型 spike 定案 → P1 输入层交付 → P2 渲染核心开工**。

### 凌晨到上午：I1 两轨审查债清账并合入

- 乙 D 轨 P1-1（阻塞项）：指派分栏同步选中并拉齐六页签数据源（`cccfe38`）、半份 run-config 如实降级显示并修分栏载入后的面板崩溃（`eac21ee`）。
- 两轨 P2 批量清：镜像 cron 通知帧无会话归属即丢弃（`b6dd93f`）、steer sink 收敛为 core `SessionSteerSink`（`7fc1055`）、`useTurnStream` 卸载清理挂起 flush timer（`ae87f63`）、e2e key 仅环境注入删源码兜底字面量（`c0162b0`）、重连恢复订阅 + resume 在途去重（`5220a33`）、SIGTERM/SIGHUP 复用幂等退出路径（`4defd0a`）、布局/草稿/偏好/元数据落盘改原子写（`3ea0b1b`）、审批模式切换真实写全局配置（`48fce5d`）。
- `/help` 同源用例改屏障式等待消满负载假红（`4e2c7a6`）→ 先合 D（`a0686fb`）后合 T（`6330f76`）→ `8c46b3c` 登记闭环。

### 上午：CI 改造——「一个包红掩盖其余」

- `ce79bf0` 把测试与检查拆成分包独立步骤，`1b12223` 合入，`cfde184` 结案。动机是之前 CI 单点红会让人看不到其它包的状态。

### 白天：交接与冒烟资产

- `7d29acb` HANDOFF/ROADMAP 快照回填到 I1 合入后的真实状态；`2a53256` 新增冒烟测试手册（启动入口 + **36 条手工用例 + 期望结果**）；`b3a0fa7` 修 CI Prettier 红。

### 晚上（15:03 起）：用户实测反馈 5 项 → P-1 阶段当天做完

- `71e4966` 登记终端 TUI 交互反馈 5 项：**子代理可见性 / 输入框置底 / 弹层位置 / 声音提醒 / 斜杠菜单**。
- 配套：`55e98eb` 阶段计划、`ee43081` grok-build TUI 对齐研究报告（5 项反馈的原版机制 + 复刻可行性）。
- 实施五连（T1~T5）：工具卡增加子会话只读入口（`27d629c`）、输入框锚底并支持鼠标滚轮/焦点事件（`9f8bc87`）、弹层统一渲染在输入框上方（`41635b2`）、**回合结束提醒（默认失焦时响铃，可用环境变量关闭）（`f62e530`）**、斜杠候选与审批提示移到输入框上方（`4f8f2ce`）。
- `3e65857` P-1 合入 main（提交自述 cli **345+2 用例**）。

### 深夜：P0 渲染层选型 spike 定案 + P1 输入层 + P2 开工

- spike 四方案实测：骨架与 10k 行合成数据生成器（`f45e7f7`）、方案 C OpenTUI（Node22 不可用 / Bun1.4 达标，`df67b5a`）、方案 A Ink 基线 + 方案 B 自研 + 方案 D ratatui（`2e39364`）、选型报告决策矩阵**推荐方案 B 自研渲染层**（`649492a`）；审查抓到 P1-1 决策矩阵四个加权值算错（66/89/68/72，C/D 排序翻转）并修复（`081df34`）。
- P1 输入层：渲染引擎无关的字节解析器 + 分发器（`71deb12`，46 例先红后绿，全量回归 **327 passed（281+46）**）、59 条键位对照表与逃生开关约定（`9705d38`）、lint error 清零（`b0be8e7`）、合入 main（`4e61cff`）；键位冲突五项按需求方裁决**采纳 grok 语义**（`f35384c`）。
- P2 开工：cell buffer / 差量刷新 / 分层布局 / 生命周期（`aca5f3b`，67 例）、T1-4 输入路径迁移至统一解析器并留 `HARNESS2_INPUT=legacy` 回退（`b008e22`，31 例）。
- 另：`46fe40f` 归档「2026-12-01 后改造方向」，冻结期不启动不提议。

## 遇到的问题与解法

（取自 `docs/issue-log/2026-09-12.md` §1–§8 与 `2026-09-12-D.md`）

- **主目录工作区同步与临时目录清理**（§1）：worktree 残留与 origin/main 落后要先处理，否则双轨合并无从谈起。
- **审查报告不入仓的口径**（承 09-11）：按 `.gitignore:13` 只留本机，避免把阶段性审查文本当交付物。
- **P0 选型审查抓出算错的加权矩阵**（§4/§5）：结论（推荐 B）未受影响，但证明「报告里的数字必须能被复算」——审查者复跑全部关键数字 ±15% 内一致才算过。
- **`/help` 同源用例满负载假红**（§2 承 09-11 尾巴）：不放宽断言，改成等 `/undo`·`/redo` 真正落帧的屏障式等待。

## 验证情况

抄自留痕，非本篇新测：

- P1 输入层全量回归 **327 passed（281+46）**，typecheck/lint 全过（`2026-09-12.md` §5）。
- P-1 合入自述 cli **345+2 用例**（`3e65857` 提交信息）。
- P2 库级交付：**全仓 1890 passed / 5 skipped / 0 failed，tsc/lint exit 0，冻结区零改动**（`2026-09-12.md` §7）。
- D 轨补丁轮收工闸门：**全仓 1260 passed + 3 skipped**（`2026-09-12-D.md` §5，其内含 core 844 / gateway 40 / desktop 304+1 / cli 72）。
- 两轨分支 CI：甲 `#67 4defd0a`、乙 `#68 eac21ee` 全绿（`OPEN.md` I1 闭环行）。
- P0 spike 的 bench 原始数字：无记录（在 `spike/` 目录，豁免门禁）。

## 关键结论

1. **声音提醒这一天是真做了的**：`f62e530` 落了「回合结束响铃（默认失焦才响、环境变量可关）」，并带 shell 级测试。2026-09-17 阶段 V0 复核确认该能力**仍在唯一在用的 next 壳里**（`next-shell.ts:388` 引入、`:4487` 构造、`:2786` 调用），且 `next-shell.test.ts:891/905/1061` 直接断言 `\x07` 发与不发——`notify.test.ts` 15 例当日实测通过。**这纠正了视觉复刻总纲 §7 V1-C「声音提醒完全没做」的判断**（详见 2026-09-17 问题日志 V0-7）：缺的不是实现，是**配置文件开关、按事件分档、去重与试听命令**。
2. 渲染层选型定案（方案 B 自研）是后面 P1~P4 全部终端工作的前提，也决定了今天 `packages/cli/src/tui/next/` 那套架构的来历。
3. CI 拆包（一个包红不再掩盖其余）是低成本高回报的基建。

## 风险与待办

- P2 只完成「库级」，`src/tui/next` 当时尚无外部引用——接线（W1~W3）是真机可用前的必要条件。
- 真机项（IME/鼠标/滚动/中断）继续挂账。
- 未 push（按授权口径），main 领先远程。

## 下一步

- 2026-09-13：P2 接线（scrollback/composer/浮层/chat-screen）→ P3 交互功能对齐 → P4 打磨（OSC52/OSC8/主题/搜索/spinner）→ D-P2 桌面补丁 → 参考基线 P0/P1/P2 开工。
