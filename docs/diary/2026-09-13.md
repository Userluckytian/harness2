# 每日日志 2026-09-13

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 遇到的问题与解法 / 验证情况 / 关键结论 / 风险与待办 / 下一步。
> **本篇为 2026-09-17 阶段 V0「基线补账」依 `git log` + `docs/issue-log/2026-09-13.md` / `2026-09-13-DP2.md` 反推补记，非当日实时撰写**；查不到的细节一律写「无记录」。

## 今日完成

当日提交 **81 个**（本仓库单日最高），时间从 00:00 一路排到 23:17。主线是**终端 grok 复刻 P0~P4 全阶段代码交付完毕**，并行推进桌面 D-P2 补丁轮、真机验收第一轮、技能目录扩展（解冻窗口 #3），晚间另起「参考基线 P0/P1/P2」三壳复刻体系。

### 凌晨到清晨：P2 接线 → P3 交互功能对齐（合入 cli 1010+2）

- P2 收口：paste 聚合空闲上限防扣键 + 补齐 rxvt/putty/linux-console 序列（`07f095d`，15 例）→ P1 合入（`84673b8`）→ T2-3 scrollback 宽字符断行/滚动模型/滚动条/差量渲染（`e16f9dd`，53 例）→ T2-4 composer（`865a275`，46 例）→ T2-5 浮层锚定与多浮层栈（`220b7e9`，61 例）→ T2-7 chat-screen 整帧装配 + 6 张整屏快照（`c0874a9`，30 例）→ 接口缺口收敛消除绘制复刻（`da9e352`）→ P2 合入（`9dcd9ac`）。
- P2-W1~W3 把 next 渲染层真正接进业务：转录投影（`f927368`，38 例）、next 输入控制器（`f8c3abd`，50 例）、**next-shell 接线带 `HARNESS2_RENDERER=next` 开关**（`3b88170`，37 例 + 真机 demo 脚本）、接线审查修 exit 兜底还原终端(P1)/焦点上报/Ctrl+D 裁决/resize 重投影（`42d2ae3`，9 例）→ 合入（`1231c61`）。
- P3 交互功能对齐：逐行配色 + 折叠键族 `e/E/h/l` + Ctrl+O→always-approve（`0f564a5`，19 例）、审批 blocking card 键位契约 + 模式四态循环（`c54b7d2`，33 例）、斜杠命令全集 + 模糊补全 + 滚轮改选（`b42ed79`，44 例）、子代理块耗时/动画 + `v` 全屏子视图（`47d6ed6`，26 例）、状态行/快捷键条上下文化 + 队列取消面板（`2a908ff`，41 例），并修审批×子视图焦点竞态等 P1（`8faeeb7`/`85ab1ff`）→ `f286b86` 合入（cli **1010+2**）。

### 上午到中午：P4 打磨（P0~P4 代码交付完毕）+ D-P2 桌面补丁轮

- P4-1 文本选择与复制（OSC52）+ 超链接（OSC8 run 包裹），双开关可回退（`279598d`，65 例）；审查 P2 修 URL 尾标点剥离/Ctrl+C 窗口/开关单源（`555ba74`）。
- P4-2 主题系统 `/theme`（dark/light）+ `/search` 命中定位高亮 + busy spinner 动画（`c3744e4`，34 例）；主题对齐用例钉 3 个字面锚抗色板漂移（`a97cb47`）→ `edc5cb3` 合入（cli **1107+2**），`cc9dfe5` 宣布 **P0~P4 全阶段代码交付完毕**。
- D-P2 桌面补丁轮（PD1~PD8 全清）：redo 冲突守卫接 UI（`f09cb77`）、F7 故障注入三断点（`e50e60d`）、F6 任务面板并行/写互斥真实 serve 观察（`471ca4b`）、F4 审批决定幂等（`6c24177`）、F8 配置写路径加固（`17dc50d`）、PD7 工作区只读列目录 IPC + realpath 越界拒绝矩阵（`3e1672c`）、PD8 关窗口决策接线自动化（`eebc87a`）→ `c267a31` 合入，`66cc7b2` 回填验收表（提交自述**编排者验收 2334+5 全绿**）。

### 下午：真机验收第一轮（照做打勾清单 + 两个缺陷）

- `5743875` 新增终端 + 桌面真机验收清单（照做打勾版，交需求方执行）。
- `4f53053` 登记真机两缺陷：桌面 dev 缺 home 导致 serve token 读不到 → 401 超时（P1）、`subagent_continue` 的 `taskId` 误导模型（P2）。
- 修复：dev 启动补齐 home（`16e8085`）→ `af287aa` 合入（提交自述「编排者复现验证」）。`subagent taskId` 那条因 core 属冻结区，转为解冻窗口待排（今天仍在 OPEN.md）。

### 下午晚些：技能目录扩展（解冻窗口 #3）与 OPEN 大结案

- 需求方 B 方案（`776e033`）→ 技能商店支持 `.agents` 文件夹型 `SKILL.md` 与四目录优先级（`b0c7c78`）+ 用例（`2288200`）→ `67af13c` 合入（提交自述 `cli skill list` 真机识别 13 个技能）；`851317d` 隔离 homedir 防兜底扫描污染真机技能。
- `38a1f3d` 给 `spike/` 目录豁免 Prettier/ESLint（P0 实验证据不入门禁）；`235cc5c` **OPEN.md 结案 6 项**（P-1/P0~P4/桌面 P2/D-P2/桌面 401/技能目录）。

### 晚间：另起「参考基线 P0/P1/P2」三壳复刻体系

- `c62721d` 新增 `docs/refs/` 参考项目复刻规范与三壳阶段计划；`16285ba` 刷新三仓基线并重分析补条目（grok-build / deepseek-harness / hermes-agent）；`a0b1f66` 加固四例 CI 时序抖动 → `93618f1` P0 合入。
- P1 内核下沉：命令注册表进 core 并导出 `describeCapabilities`（`ec13367`）、cli 命令分发统一接 core 消三处内联（`7137533`）、消三条遗留 lint warning 回到 46 基线（`85520ef`）→ `8dd8820` 合入。
- P2 CLI 骨架：渲染模式层与输入焦点层（`06fbddb`）、双渲染模式与 Esc 新规格接线（`e09bfdb`）、补 G-01~G-20 覆盖缺口十一例（`1464060`）→ `bd77216` 合入。

## 遇到的问题与解法

（取自 `docs/issue-log/2026-09-13-DP2.md` §1/§4 与 `2026-09-13.md`）

- **D-P2 每任务「先红后绿 + 变异验证」**：§1 明确逐任务做法，§3 做边界自检（冻结区是否被顺手改）。
- **桌面 401 超时**：根因是 dev 启动没给 home，token 读不到；不是鉴权逻辑缺陷——先用真机复现确认再改。
- **审查取证的 dist 陷阱**（当日定出口径，09-14 正式登记）：不得对可能过期的 `dist/` 下结论，引用实现行为前先 `pnpm -r build`。

## 验证情况

抄自留痕，非本篇新测：

- `2026-09-13-DP2.md` §2「总闸门（合并 main 后全仓实测，exit code 实录）」：**core 844 passed + 2 skipped（61 files）· gateway 40 passed（14 files）· desktop 343 passed + 1 skipped（44 files，基线 304）· cli 836 passed + 2 skipped（63 files）**。
- P3 合入 cli **1010+2**、P4 合入 cli **1107+2**（基线 345 → 1107），并记「四包闸门全绿；core/gateway/desktop 零改动（diff=0）」（`2026-09-13.md` §1/§2）。
- `66cc7b2` 提交自述编排者验收 **2334+5 全绿**（全仓口径）。
- bench：p95 0.077~0.085ms（门槛 33ms）在 P2/P4 两处留痕，无退步。
- 当日 CI run 编号：无记录（`2026-09-13-DP2.md` §5 有 CI 追踪补记，具体 run 未抄进 diary 口径）。

## 关键结论

1. **终端 grok 复刻 P0~P4 当天代码交付完毕**，cli 用例从 345 涨到 1107（3.2 倍），且全程 core/gateway/desktop 零改动——冻结契约经受住了单日 81 提交的并行强度。
2. 一天之内两条主线（终端 P 系列 + 桌面 D-P2 + 新开的 refs P 系列）能并行，靠的是 09-11 的契约冻结与 09-12 的 CI 拆包。
3. 真机验收第一次拿到「照做打勾清单」这种可交付物，当天就抓到两个真缺陷——**离线闸门替代不了真机**。

## 风险与待办

- 真机清单剩余项仍在需求方手里（鼠标/选择复制/IME/主题/fullscreen 往返/Esc 五项）。
- `subagent_continue` 的 `taskId` 误导问题需解冻窗口，未排期。
- 当日 22 个提交「main 领先 origin 未 push」（按授权口径）。
- 新开的 refs P0~~P4 三壳复刻体系与刚交付完的终端 P0~~P4 之间会有大量重叠条目，需在 P3/P4 阶段明确边界。

## 下一步

- 2026-09-14：P3 CLI 命令面/minimal → P4~~P6 桌面三页 → P7 核心能力 H-11~~H-46 → P8 共享 UI 包与 web 壳 → P9 收口。
