# 每日日志 2026-09-14

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 遇到的问题与解法 / 验证情况 / 关键结论 / 风险与待办 / 下一步。
> **本篇为 2026-09-17 阶段 V0「基线补账」依 `git log` + `docs/issue-log/2026-09-14.md` + `OPEN.md` 反推补记，非当日实时撰写**；查不到的细节一律写「无记录」。

## 今日完成

当日提交 21 个，全部集中在 **14:13–23:46**（14:13 之前无提交记录）。一天跑完 refs 复刻体系的 **P3 → P9 七个阶段**：CLI 命令面、桌面三页（骨架/对话/轨迹+设置）、核心能力补齐 H-11~H-46、共享 UI 包 + web 壳、P9 收口。

### P3 CLI 命令面与 minimal 基座（14:13–14:19）

- core 命令面扩到 23 条并新增 `ui`/`scrollback` 配置段（`b9a8b8b`）；cli 落命令面/卡片/队列/状态行/minimal 与键位归存（`68d55d3`）；回填 G-21~G-90 状态列（`7976b47`）→ `0a13205` 合入。

### P4~P6 桌面三连（15:22–18:36）

- P4 桌面骨架：slot 四席位三栅布局 + 侧栏取代旧分屏（`651348b`）、回填 D-01~D-25（`4af820c`）→ `ee5f4d7`。
- P5 桌面对话页：视图环与 composer 接进对话席位（`44c4e3d`）、回填 D-30~D-39（`1c6bb12`）→ `5c80632`。
- P6 桌面轨迹页/模型配置页/设置壳与审批工具卡（`eb4c3f8`）、回填 D-40~D-59/D-85/D-86（`2b46b8e`）→ `2b0c53b`。

### P7 核心能力补齐 H-11~H-46（20:54–21:23）

- 主体落地（`479d8ab`，当时标 `🚧wip` 待修 P0 后合入）+ 登记缺口（`34e6f3b`）→ 逃逸用例补至**九种手法**（`eccc119`）→ `f7d0ae2` 合入。

### P8 共享 UI 包与 web 壳（23:06–23:12）

- `0b8f9ff` 共享 UI 包与 web 壳（提交自述**浏览器真机一轮对话通过**）→ `c7c76a0` 合入。

### P9 收口（23:41–23:46）

- `4febde8` 归存补齐/索引刷新/上层文档/冒烟新用例 → `83f2e9b` 合入。P9 的成果是**三份 refs 文档各加「归存汇总」小节**：95 条 G-/63 条 D-/54 条 H- 编号全部有归属（✅/🟡/⬜/➖/待人类决策 五类），不再有无主条目。

### 同日并行：P3 修复棒与 P3-F 键位归存（记在 `2026-09-14.md`）

- G-46 状态行首个定时器无来源（P1）：补 `bootstrapStatusLineTimers()` + `governor.test.ts` 自举 4 例 + `p3e-gaps-wiring.test.ts` 真实子进程 2 例，**变异验证**（注释掉自举 → 两例 waitFor 超时必红）。
- G-84 `/theme（/t）` 别名不可用（P2）已修。
- G-32~G-41 六组 Agent 级键位逐条归存 P7（键位已登记占用）+ Ctrl+X 冲突修复。
- P7-B：H-11 全文检索 / H-12 压缩分层 / H-13 可移植 / H-14 自动标题 落 core 并有用例，**三壳命令接线归存后续**。

## 遇到的问题与解法

（取自 `docs/issue-log/2026-09-14.md` 与当日进 OPEN.md 的观察项）

- **审查对过期 `dist/` 取证，误报「vm 沙箱逃逸 P0」**：P7 审查引 `packages/core/dist/tools/script.js`（过期产物）下结论，编排者按源码复核确认 `script.ts` 早已改 `spawn` 子进程、九种逃逸手法全过。当天定出口径并写进约定：**审查引用实现行为前必须先 `pnpm -r build`，禁止对 dist 下结论**（同类问题在共享包 `ui-shared/dist` 上也登记了一条）。
- **core 测试固定占 46213 端口**：编排者验 P8 时亲历与开发用 `harness2 serve --port 46213` 撞端口导致三例 `EADDRINUSE`，杀进程后复绿 1232+2 → 登记为技术债（建议改 `--port 0`）。
- **桌面启动早期 `serve 未就绪` invoke 竞争**：每次启动 5~8 条错误后自愈，`smoke` 最终 exit 0 → 根因未定位，明确「勿凭猜测改」，挂账。
- **web 壳 token 三条暴露面**：构建期内联 / URL `?token=` / WS 握手 `?token=`，结论「web token 仅限本地或受信网络，生产须同源反代」→ 补进 `docs/API-STABILITY.md`（如实登记，未伪装修好）。
- **P5/P6 能力缺口全部如实降级登记**，未伪造：图片字节通道缺失标 `IMAGE_TRANSPORT_MISSING`、桌面缺 `[ui]` 配置通道、D-55 发现模型需先存密钥、跨进程设置写入无锁 TOCTOU、`redactSecrets` 对裸密钥覆盖不全（已一行兜底加固）。

## 验证情况

抄自留痕，非本篇新测：

- `docs/issue-log/2026-09-14.md` §1 状态行：「本机闸门全绿：build/typecheck 0、eslint 0 error/46 warning、prettier 全绿、**harness2 1654 passed + 2 skipped**；待阶段独立验收」（`harness2` = cli 包名）。
- OPEN.md P8 行：编排者验 P8 时杀进程后 core **1232 + 2** 复绿。
- P7 逃逸用例：补至九种手法（`eccc119`），当日记录为链全过。
- P8 桌面/web 端到端：提交自述「浏览器真机一轮对话通过」，具体自动化数字：无记录。
- P9 收口：三份 refs 归存计数 95（44/43/5/3/0）、63（40/7/15/1/0）、54（9/14/22/8/1），出自 `OPEN.md` 的 P9 收口行。

## 关键结论

1. 一天做完七个阶段，代价是**留下大量「登记未修」的诚实降级项**（图片通道、配置通道、跨进程锁、裸密钥擦除）——这些是后续阶段的真账，不是已完成。
2. P9 的「归存」比 P3~P8 的功能更值钱：它把 212 条编号从「谁也没认领」变成「五类归属 + 理由」，这正是 09-17 视觉总纲要的记分口径。
3. dist 取证误报暴露一个反复踩的坑：**过期的编译产物会替人撒谎**。当天定的口径值得长期保留。

## 风险与待办

- H-44/H-66~H-68 flows 与自然语言 cron（H-46）**三壳零接线**（core 契约与用例齐备），归存 P8/P9 未做。
- H-42 `/replay` 入口、H-12 loop 自动压缩切分层、H-22 自动提炼 turn 未接 → 归存 P8/P9。
- CLI 与 serve 工具装配四段重复（技术债）。
- 桌面启动 invoke 竞争、web 会话列表不实时刷新、web Enter 提交未在自动化路径复现（需真机）。
- 当日 21 提交未 push（按授权口径）。

## 下一步

- 2026-09-15：P10「next 单轨化清场 + grok 抓屏对照」——删旧终端壳（含 ink 整棵依赖树）、建 `scripts/tui-parity/` 对照台，并首次逐场景与真 grok 比版面。
