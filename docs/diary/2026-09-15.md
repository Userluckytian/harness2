# 每日日志 2026-09-15

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 遇到的问题与解法 / 验证情况 / 关键结论 / 风险与待办 / 下一步。
> **本篇为 2026-09-17 阶段 V0「基线补账」依 `git log` + `docs/issue-log/2026-09-15.md` 反推补记，非当日实时撰写**；查不到的细节一律写「无记录」。

## 今日完成

当日提交 13 个，**全部落在 22:00–23:29**；22:00 之前只有 `2026-09-15.md` 问题日志里的两项闭环（文档口径纠偏、grok 能力确认），**当日早段是否有其它工作：无提交记录，故本篇不写**。

主线是 **P10 阶段：next 单轨化清场 + 双 TUI 抓屏对照台落地**。

### 22:00 两项口径与计划先行

- `c8066dd` 改「图片任务」协作口径：`AGENTS.md` §视觉分析 原写死「主力模型无视觉 → 优先调 `@vision-analyst`」，实测主模型（`deepseek-flash`）能直接 `read` 图片并 OCR 出内容，旧口径会无条件多派一层子代理。改为**三步**：主模型先自测 → 自测失败才兜底子代理 → 用户点名则直接派；同步改 `.opencode/agents/vision-analyst.md`（并把过期的模型名对齐为 `DeepSeek Flash`）与 `.opencode/scripts/vision-bridge.py`。
- `48fca36` P10 阶段计划入库（`2026-09-15-phase-next-only-and-grok-parity.md`）。

### 22:04–23:22 P10-A 旧终端壳单轨化清场（A1~A8）

- 需求方原话是「彻底删掉旧渲染路径，**不让它在项目里出现任何字眼**」。
- A1 旧壳零引用盘点（`6b45ccc`）→ A2 删除旧终端渲染路径（`a1c7c92`）→ A3 终端装配收敛为 next/piped 双路（`0f1bb9e`）→ A4 清理旧壳测试并登记覆盖迁移评估（`93bce85`）→ A6 去痕命名与注释清理（`aee4881`）→ A5 抽出 `turn-events` 纯函数、删旧壳死 hook（`d73304b`）并移除旧壳运行时依赖（`edcaf19`）→ A7 现行文档清场（`fb8e5b9`）→ A8 零残留验证与 OPEN 同步（`231c265`）。
- 规模（据 `2026-09-15.md` §3）：删 14 个 `.tsx` + 3 个桥接模块 + 20 个测试文件；**177 条旧断言逐条登记为 ✅81 / ➖13 / ⚠️83 缺口**（没有假装等价迁移）；移除 `ink` 整棵依赖树（35 包）、`react`、`@types/react` 与 `HARNESS2_RENDERER` 开关；core 只改两处注释去痕，**导出面基线 diff = 0**（没越冻结区）。

### 23:29 P10-B 抓屏对照台落地

- `033892f` 落 `scripts/tui-parity/`（ConPTY + pyte + Pillow，18 场景 / 12 组）与 `docs/tui-parity/`（README 方法与诚实边界、matrix 逐项差异表）。
- 当日两项能力确认（`2026-09-15.md` §2，实测非推断）：`grok 1.0.13 (5e9a58528b76)`；`grok -p "…" --output-format plain` 可无头驱动、退出码 0；**交互式 TUI（裸 `grok`）无 TTY 不可代驾**；grok 是独立会话、看不到本会话上下文、每次消耗用户额度。
- 关键边界写进 README：**PNG 是 ConPTY 抓屏 + 本地重绘，不是系统级截图；结论一律以 `.txt` 屏幕网格为准**；鼠标/选择复制/IME/系统字体覆盖不到，仍须真机。

## 遇到的问题与解法

（取自 `docs/issue-log/2026-09-15.md`）

- **「无视觉」被写成普遍规则**：旧口径源自脚手架对默认模型的假设。教训是**能力判定要看当前会话实际模型**，且反幻觉约束（看不到就不猜、不确定标「不确定」）与自测规则并存，不能互相替换。
- **grok 交互式 TUI 不可代驾**：改走「无头 `-p` + 由抓屏台驱动真实 TUI 进程（ConPTY）」两条路，前者取内容、后者取版面。
- **额度约束**：grok 侧默认禁跑（必须显式 `--allow-grok`，`--grok-budget` 默认 1），本轮 grok 只跑 A1 冷启动一帧即杀，其余场景只定义不执行。

## 验证情况

抄自留痕，非本篇新测：

- P10 收口闸门（`2026-09-15.md` §3 状态行）：本地全绿，**cli 1520 passed**、lint 0 error + prettier 全绿；**未 push / 未合 main**。
- 旧壳删除的「零残留」判据：`grep` 白名单外 0 命中（同日 A8 提交）。
- grok 无头能力：`which grok` → `/c/Users/ASUS/.grok/bin/grok`；`grok --version` → `grok 1.0.13`；`grok models` 默认 `40080-muse-spark-1.3-contributor`、可选 15 个；实调 `-p` 输出「可以调用」exit 0。
- 抓屏台冒烟（README §8 记为 **2026-09-15 本机实测**）：我方 3 场景 exit 0 / 26.4s / 无空屏；A1 连跑 3 次首屏一致（`quiet≈3.0s`）；D1 走 `/exit` 子进程自退 exit=0；6 场景 12 张 PNG 无空屏；grok 侧 A1 一帧 exit 0 / 5.2s / 53735 B。

## 关键结论

1. 仓库从此**只有一条终端渲染路**（next 壳 TTY + piped readline 非 TTY 回退），双轨并轨的所有分歧一次终结；代价是 83 条旧断言成为已知缺口，已如实登记而非「等价迁移」。
2. 有了抓屏台，「像不像 grok」第一次变成可反复取证的东西——这是 09-17 视觉总纲能量化验收的前提。
3. 09-15 定的两条口径（主模型先自测视觉、结论以 `.txt` 为准）今天仍在生效。

## 风险与待办

- P10/P11 分支都在本地，**未 push、未合 main**（等授权），CI 七 job 待此后再验。
- 抓屏台覆盖不到的真机项照旧挂账。
- mock 复现不了的 A2/E1/L2 标 `defined_only`，L1 的 grok 侧剧本待重写。

## 下一步

- 2026-09-16：跑 P10-C 双 TUI 逐场景对照并出 P0/P1 改进清单；据此开 P11「终端界面拉齐」；当晚登记 P12 待办。
