# 每日日志 2026-09-16

> 用途：日常进度记录；发版时作为 git-release 信息素材。格式：当日完成 / 遇到的问题与解法 / 验证情况 / 关键结论 / 风险与待办 / 下一步。
> **本篇为 2026-09-17 阶段 V0「基线补账」依 `git log` + `docs/ai-framework/plans/2026-09-15-phase-p11-tui-polish.md`（编排者验收记录）+ `docs/ai-framework/plans/2026-09-15-p12-backlog.md` 反推补记，非当日实时撰写。**
> **诚实提示：当日 `docs/issue-log/` 没有 2026-09-16 文件**（09-15 之后直接跳到 09-17），所以「遇到的问题」一段只能引计划与提交里的留痕；计划里也没写的细节一律标「无记录」。

## 今日完成

当日提交 22 个，时间跨 **00:01–02:12**（P10 收尾 + P11 全程，一个通宵）与 **08:33–08:40**（P12 登记）。这是「终端界面拉齐 grok」真正落地的一天。

### 00:01–00:17 P10-C 双 TUI 逐场景对照报告

- `c3e5700` 出对照报告与 P0/P1 改进清单；`df14329` 修正 A 组版面为 next 实测并新增输入区 P1；`8b1a25d` **用 big-pickle 补抓 grok 四组场景**（I/J/K/L 组）并回填结论—— grok 侧基准帧由此从 1 个场景扩到 13 个。
- 对照得到的关键事实（进 `docs/tui-parity/matrix.md`）：grok **没有 `/undo` `/redo`** 而是 `/rewind <prompt>`；提示行随状态改写（忙碌态 `Enter:queue … Esc:cancel`）；状态行显模型名 `Big Pickle · always-approve`；未识别 `/xxx` 交给技能/命令路由而非报错。

### 00:17–02:12 P11「终端界面拉齐」（修 1 个 P0 + 6 项 P1）

- `2c87f59` 立 P11 计划；`14c5ffa` T0 对照问题定位盘点。
- **T1 修 P0**：`/help` 面板渲染错位（`d6b0d16`）。根因（据计划验收记录）是多行文本被投影成一条逻辑行、换行符被当可打印字符写进单元格；修法为两层防御 + **6 条版面级断言** + 变异验证。
- **T2/T3**：输入区 `❯` 锚点 + 空态占位、候选列表两列化（`71eb550`，含 `displayWidth` 截断残留修复）。
- **T7**：冷启动引导卡（`d879700`，默认一次性、任意键收起、`HARNESS2_NO_WELCOME` 可关）。
- **T4/T5/T6**：每轮耗时与 token 用量计时、工具行人类化（JSON 参数入展开态）、忙碌态耗时与取消反馈（`97ced01`）。
- 修复棒（独立审查提出 4 项 P1 的闭环）：会话切换重置用量记账 + 写入缝真装配用例（`41582cb`）、修正变异记录与 `displayWidth` 并补 minimal 引导卡用例（`048bbf2`）、闸门格式化 8 文件（`d7f346f`）。
- 另一处 P0 同类隐患：残留 `CellBuffer` 统一降级 C0 控制字符，`\t`/`\x1b` 不再逐格发射（`0f7657c`）。
- `cb477d2` 收口：前后对照、清单回填、OPEN 关闭与阶段验收记录。

### 08:33–08:40 P12 交接与证据入库

- `359bdc9` P12 待办与交接文档 + HANDOFF 状态同步；`353b0b2`/`d9a7717` 把关键证据与 K 组图**拷进 `docs/tui-parity/images/` 入库**（原因写得很直白：`out/` 被 gitignore，同事看不到）。
- `aecd176` 登记 C6：用户 09-15 问的「面板有没有背景色、匹配项会不会变色」——实测**全链路 fg-only**（`CellBuffer` 无 bg 属性位、`DiffPresenter` 只发前景 SGR、全仓无 `48;2`），且 **grok 同样无背景色** → 判定为「超出参考实现的自选增强」而非 parity 缺口，逐字符匹配高亮可先做、背景色需动 presenter 属性模型建议单独阶段。

## 遇到的问题与解法

（当日无 issue-log 文件，以下取自计划验收记录与提交正文）

- **审查者误判 prettier 基线**：P11 独立审查报「lint 闸门实际是红的」为 4 项 P1 之一，编排者复核认定属**误判**（审查者用 stdin 管道检查，未加载项目 `.prettierrc`）。教训：跨工具取证要带配置上下文。
- **报告口径自我纠正两次**：裁定 1 承认 `matrix.md` 原写「我方提示行恒定不变」**有误**（`chat-screen.ts` 的 `shortcutsFor()` 确有 busy 分支，初版结论源于 G1 抓帧时 mock 回合已结束的时序假象）；`fb99b52` 另修两处口径（忙碌态提示行/状态行位置）。
- **`text.length` 当显示宽度**（裁定 1 并入 T3）：改 `displayWidth` + 宽字符不切半，两处变异验证均红。
- **`\t` 的取舍**（裁定 2）：`CellBuffer.setCell` 统一降级 C0，`\t` 定为**转空格、不按 tab stop 推进**，理由与幂等契约写进代码注释；裁定 3 接受引导卡「一次性」只在进程内（不落盘）。
- **抓屏台已知坑**：`B1` 场景重抓时仍超时被强杀（但已出帧）——登记未修。

## 验证情况

抄自计划「编排者验收记录」与 HANDOFF（当日实跑，非本篇新测）：

- 第一批（T0+T1）：`pnpm --filter harness2 test` = **91 files / 1526 passed + 2 skipped**（P10 基线 1520 → +6 条版面断言）✅
- 第二批（T2/T3/T7）：**91 files / 1553 passed + 2 skipped**（+27）✅
- 第三批（T4/T5/T6）编排者独立复核：越界 `packages/{core,gateway,ui-shared,desktop,web}` = **0 文件**（core diff 为空）；cli **92 files / 1571 passed + 2 skipped**（+18）；并**读图确认** `T6-busy-after.png` 里时间戳/`⠧ 运行中… 已用 2s`/提示行/输入区锚点四处同时生效。
- 修复棒复核：`pnpm lint` exit 0（eslint 0 error / 46 warnings + prettier 全绿）；cli **93 files / 1575 passed + 2 skipped**；越界 0；`git status` 干净。
- T8 收口：`pnpm -r build` / `pnpm -r typecheck` 六包全绿；重抓 `run.py --side ours` 出图 16 个（B1 超时强杀但已出帧）→ `images/p11/before-after.md`。
- 全仓口径（当日 P11 记录，2026-09-17 V0 复跑**逐项一致**）：core 1232 + 2、gateway 40、ui-shared 70、web 36、desktop 930 + 1（**94/96 files，2 个红文件为预存环境问题**：`serve-manager.test.ts` 需 `127.0.0.1:46213` 空闲、`settings/models/*.test.tsx` 的 jsdom `node:` 报错，**已用 `main` 同环境对照证明非本阶段引入**）。
- 当日 CI：无记录（两分支均未 push，等授权）。

## 关键结论

1. 阶段结论 **✅ 通过**：P0 修完且带版面级断言与变异验证、6 项 P1 全做完并有前后抓屏、审查 4 项 P1 闭环、闸门全绿、越界 0。
2. **P10/P11 都完工但停在本地分支**：`feat/phase-p11-tui-polish` 叠在 P10 上、领先 `main` 33 个提交未合入未 push。这直接导致 09-17 立项视觉总纲时，`main` 上连抓屏台都不存在——**这是当天没写日志之外更容易伤人的第二笔欠账**。
3. C6 是本轮少见的「用证据把一个感觉问题定性掉」：领导问的背景色，答案是**参考实现自己也没有**，于是从「复刻缺口」改判为「自选增强」，省掉一整条无底洞。

## 风险与待办

- P12 待办清单已成型但**未排期**：C1 状态行缺模型名/git 分支、C2 会话 id 常显、C3 未知命令语义待定性、C4 `/rewind` 别名、C5 子任务卡补耗时步数（我方优势项）、C6 逐字符高亮（可先做）与背景色（建议单独阶段）。
- E 审批弹窗与 L2 模型报错仍是 `defined_only`；L1 的 grok 侧剧本待重写；K 组逐格折行核对未完。
- 真机项（鼠标/选择复制/IME/主题/fullscreen 往返/Esc 五项）全部待人类。
- 合并与 push 等授权；CI 七 job 待此后再验。

## 下一步

- 2026-09-17：立「视觉复刻总纲 V0–V6」，先做 V0 基线补账（本篇与 09-10~09-15 共 7 篇 diary 即为其中 V0-1 项）。
