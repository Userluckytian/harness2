# 开放事项索引(未关闭项) — harness2

> 规则：每天开工先读本文件掌握未完成事项；每天收工把当天仍未关闭的项同步进来，已关闭的移出。
> 状态：待处理 / 修复中 / 已修复待验证。已关闭项不在此文件，历史留在各日期日志。
> 元约定：`docs/issue-log/README.md`（AGENTS.md 强制遵循第 6 条）

| 日期 | 事项 | 状态 | 详情 |
|------|------|------|------|
| 2026-09-07 | **桌面端白屏已修复（vite base 相对路径）**：vite 默认 `base:'/'` 产出 `/assets/...` 绝对路径，Electron 经 file:// 加载 `index.html` 会拼成 `file:///assets/...` → js/css 全打不开 → 白屏。冒烟只看 `did-finish-load` 与 `window.harness2`（preload 独立运行），**查不到 React 是否真挂载**，故冒烟全绿仍白屏。修复：`vite.config.mts` 设 `base:'./'`，产物改 `./assets/...`。已用临时 Electron 诊断验证 React 挂载（`.app` 渲染、无 console 报错）、桌面冒烟回归绿 | 已关闭（本地+远程验证） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **v1.0.0 发布动作待授权（阶段 12 物料已就绪）**：四包版本已升 1.0.0、CHANGELOG 1.0.0（M1-M4 总述）、README 终稿（快速开始命令已实跑核对）、ROADMAP v1.x 展望（草案未批准）。执行前待人类（清单 = `docs/RELEASE-CHECKLIST.md` ③）：①npm 包名占用检查（`npm view harness2` / `npm view @harness2/core`）；②配置 `NPM_TOKEN` secret；③推 tag `v1.0.0`（合并发布，跳过 0.1/0.3/0.6 独立 tag，release note 说明里程碑对应关系）；④GitHub Pages 开启（**2026-09-07 已于 API 完成**：repo 改公开 + Pages source=GitHub Actions 并上线）。**push 推送（备份）已授权并持续执行** | 待处理（需人类操作：npm 包名占用检查 + 配 NPM_TOKEN + 推 tag v1.0.0） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **GitHub Pages 已开启并上线（无需手动操作）**：2026-09-07 经 API 完成——repo 改公开（visibility=public）+ Pages 来源设 GitHub Actions（build_type=workflow）；pages job 部署成功，站点 https://userluckytian.github.io/harness2/ 返回 HTTP 200，docsify 正常渲染（title「harness2 文档」）。原「需人类在 Settings 操作」前提已由 API 代劳；渲染级验证随首次部署完成 | 已关闭（验证通过） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **CI 全绿（run#22 success）：六处修复全部验证通过**。①typecheck 依赖 build 产物→前置 `pnpm build`（ci.yml & release.yml）；②pages 分支 `master`→`main`；③chromium 安装 `--filter @harness2/core exec playwright`（根目录找不到二进制）；④pages job 权限补 `contents: read`（默认被 pages:write 覆盖致 checkout 404）；⑤linux AppImage `executableName`（scoped 包名派生 @harness2desktop 含 @ 非法）；⑥平台相关测试——doctor 写死 `platform: win32` 改动态、crash-drill 用 SIGKILL（Unix SIGTERM 触发优雅删锁）。另 repo 改公开、Pages source=GitHub Actions 已启用并上线。远程 run#22 全部 job 绿 | 已关闭（远程验证通过 run#22） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **README 截图占位待补**：README 顶部已留 HTML 注释占位（终端 chat 流式/工具行、桌面端多会话分屏、traj 时间线三张）；截图需真机环境（mock 或真实 key 会话 + 桌面端），补图后删除注释占位 | 待处理（需真机环境） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **CLI 侧子会话工具集防回归测试的漂移风险（阶段 11 审查 P2-3 留档）**：subagent.test.ts 的「CLI 形态防回归钉死」用例用**模拟注册表**（手工 mimic chat.ts 的装配：builtin+memory+browser+skill）断言子会话剔除 per-session 绑定类工具——若未来 chat.ts 新增其他 per-session 工具而测试注册表未同步，测试不会红（漂移风险）。serve 侧已有真实 hub 集成测试（toolsForSession→subagent_start 全链路）不受影响。后续：把 chat.ts 基础注册表构建提炼为可测函数后，CLI 用例改用真实装配 | 已知限制（留档，提炼重构待后续阶段） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **阶段 11 残留手工验收（待用户环境）**：①真实大会话跑 bench 复核基线数字——`pnpm build && pnpm bench`（或 `H2_BENCH_EVENTS` 自定规模）在用户真实负载环境复跑，对照 architecture.md「性能预算」表；②`harness2 doctor` 实机输出核对（真实 config/auth/MCP/sessions 环境下 OK/WARN/FAIL 判定与脱敏展示）；③崩溃报告实机演练（`~/.harness2/crash/` 落盘、内容脱敏、无遥测声明） | 待处理（需人类操作：用户环境复核） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-07 | **三平台桌面包 CI 产物已远程验证通过**：run#22 中 build-desktop 矩阵 win-setup / mac-dmg / linux-appimage 全部 success；linux AppImage 曾因 `executableName`（含 @）失败，已修 electron-builder.yml（executableName: harness2）后通过。剩余：release.yml 的 tag 触发发布（需 NPM_TOKEN + push tag） | 已关闭（远程验证通过 run#22） | [2026-09-07.md](2026-09-07.md) |
| 2026-09-06 | **阶段 10 残留手工验收清单**（自动化已覆盖逻辑路径；待用户环境）：①真实长会话导出体积评估——用真实跑过若干天的 `~/.harness2/sessions/` 大会话跑 `harness2 export`（观察 zip 体积/耗时，全量内存口径是否可接受）→ `harness2 replay` 报告可用性；②skill 真机体验——`.harness2/skills/` 放真实 skill → `skill list` → chat 里确认「[Skills 可用]」注入、模型按需调 `skill` 工具加载全文的行为质量（是否恰当取用/不滥用） | 待处理（需人类操作：用户环境） | [2026-09-06.md](2026-09-06.md) §18 |
| 2026-09-06 | **v0.6.0（M3）发布待授权**：~~代码/物料全部就绪~~ → **由 v1.0.0（2026-09-07 行）合并发布取代**：0.1/0.3/0.6 不推独立 tag，直接发 1.0.0 并在 release note 说明里程碑对应关系；本行保留作为 M3 发布素材的历史索引（插件/MCP/subagent、网关、导出回放+Skills、CHANGELOG 0.6.0、README 章节） | 已被取代（并入 v1.0.0 发布动作） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 9 真机联调清单**（待用户 QQ 开放平台/飞书凭据）：①auth.json.gateways 配 appId/appSecret → ②`harness2 gateway` 启动（serve 自动起）→ ③QQ 群 @bot 对话、私聊对话（token 刷新/心跳/断线重连真机观察）→ ④审批请求回复「1/2」→ ⑤消息频率限制与 msg_seq 递增观察 → ⑥飞书 webhook（公网/穿透）+ verificationToken 校验 + p2p/群 chat_type 判定补全 | 待处理（需人类操作：凭据 + 真机） | [2026-09-06.md](2026-09-06.md) §17 |
| 2026-09-06 | **阶段 9 复审待补（fail 闭环条件）**：审查 verdict 曾为 fail（P0 网关聋哑 + 7 P1），修复已落地（`fb837bb`，全量 540+1 绿）——**基础设施恢复后补一轮复审**确认 P0/P1 修复有效性（重点：startGateway 生命周期断言、重连重订阅、msg_seq 递增） | 已修复待复审 | [2026-09-06.md](2026-09-06.md) §17 |
| 2026-09-06 | **阶段 8 残留手工验收清单**（自动化已覆盖逻辑路径；待用户环境）：①真实 MCP server 实测——config 配 `mcpServers`（如 filesystem）→ `harness2 mcp list` 探测 → chat/serve 中 `mcp__<server>__*` 工具真实调用；②第三方插件样例从零装载——`~/.harness2/plugins/<name>/` 手写 manifest+index.js → `plugin list` → `plugin enable` 审批 → 重启后工具/事件订阅生效；③桌面子会话跳转体验——subagent 工具行「子会话 ↗」跳转 traj 实机手感 | 待处理（需人类操作：用户环境） | [2026-09-06.md](2026-09-06.md) §15 |
| 2026-09-06 | **阶段 8 独立审查待补**：~~实现代理单飞完成 Task 1-5（本日无中断），尚未跑独立只读审查~~ 独立只读审查已完成（2026-09-06）：P1×4（MCP 双重调度竞态 / maxTurns 契约矛盾 / 插件抢占 subagent 工具名崩 chat / 子会话零快照）+ P2×6 + 补测×3 **全部修复并附防回归测试**（527 项 526 passed + 1 skipped，见日志 §16）；剩余：正式 `/accept-phase` 验收待做 | 部分关闭（审查发现项已修；accept-phase 待做） | [2026-09-06.md](2026-09-06.md) §16 |
| 2026-09-06 | **cron 记账边角（阶段 7 审查 P2-7，评估不修留档）**：①`lastRunAt` 记 tick 扫描时刻而非执行完成时刻（长任务时记账偏早）；②tick 扫描与执行之间被 `remove` 的任务照跑、执行后记账丢失（`store.update` 找不到任务即静默丢弃）。影响：记账时间与真实完成时间有偏差、运行中删除任务时该次执行历史仍落盘但任务状态不再更新——均属边角场景（任务执行时长接近 tick 周期 / 运行中删除），at-most-once 主语义不受影响。重启条件：cron 承接长任务（Ph8 subagent 附近）或需要精确 `lastRunAt` 时，改 `executeAndRecord` 的记账时点 | 已评估不修（留档） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **v0.3.0（M2）发布待授权**：代码/物料全部就绪（桌面端 nsis 安装包、浏览器工具、上下文压缩、定时任务、信任域加固、CHANGELOG 0.3.0、版本号三包已升）。执行前待人类：①创建远程仓库并授权 push；②npm 包名占用检查（`npm view harness2` / `npm view @harness2/core`）；③配置 `NPM_TOKEN` secret；④推 tag `v0.3.0`（NPM_TOKEN 缺失时 publish 条件跳过不会误发）。可与 v0.1.0 合并发布（跳过 0.1/0.3 双 tag，直接发 0.3.0 并在 release note 说明） | 待处理（需人类操作：授权 + NPM_TOKEN） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 7 独立审查待补**：实现经编排者接手收尾（子代理三次并发限制中断——Task 1-3 实现代理完成、Task 4-6 编排者代做留档）。~~待基础设施稳定后对 Ph7 补独立审查~~ 独立审查已完成（2026-09-06）：P1×1 + P2×5 全部修复并附防回归测试，P2×2 评估不修转 OPEN 登记（见本表 cron 记账边角 / loop.test 抖动两条），修复记录见日志 §14 | 已关闭（审查完成，发现项已修复/登记） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 6 真实模型记忆三态验证**（待 key）：①off（缺省）模型看不到 memory 工具、无注入；②ask：模型记忆写入进 pending → `harness2 memory pending` → `approve <id>` 落盘 → 新会话注入生效；③auto：直接写入并在新会话生效；④nudge 复盘（roles.small）真实触发且不阻塞主对话；⑤记忆文件手工改坏 § 结构后写入被拒并有 .bak。与「M1 真实模型 chat 手工验收清单」合并执行 | 待处理（需人类操作：提供 key） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 6 独立复审**：独立审查已完成并产出发现项——P1×2（serve 入口 / nudge 复盘记忆注入漏传）+ P2×5 + 覆盖缺口×2，**全部修复并附防回归测试**（390 项 389 passed + 1 skipped，见日志 §12）；剩余：正式 `/accept-phase` 验收待做 | 部分关闭（发现项已修；accept-phase 待做） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **pending 暂存上限 200（审查修复 P2-3 新口径登记）**：ask 模式暂存条数达 200 后写入被拒并提示先处理（`approve`/`reject` 逐条处理，或 `harness2 memory pending --clear` 清空全部并输出条数）；与「只延迟不丢弃」口径不冲突（上限是显式拒绝，需人工处理后重写）。测试/真实模型验证清单中涉及 pending 的步骤不受影响 | 口径登记（新行为，无未决项） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 5 独立复审待补**：审查代理连续 4 次因基础设施故障失败（captcha/Model request），本阶段由编排者代审（verdict: pass，命门 `EventMirrorWriter` 经查为真实 writer 观察包裹、单写者不变量成立）。2026-09-06 已补一轮复审并修复其发现项（P1 serve-manager 重启语义 + P2×6，全部附防回归测试，见日志 §13；2 项评估暂不修已留档）；**子代理独立复审仍待基础设施恢复后补做**（可与 accept-phase 合并） | 部分关闭（补复审发现项已修；子代理独立复审待做） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 5 补复审不修留档（2 项，均低危）**：①**WS 增量 rewind 影子**——WS 增量事件帧镜像落盘事件（含 rewind/marker），undo 后影子折叠依赖客户端全量重放/整体替换路径（桌面端 chat-model 为全量重投影天然成立）；纯增量消费者的影子口径本轮评估暂不修，留档；②**本地信任域加固**——serve HTTP/WS 仅监听 127.0.0.1、无鉴权，未做 Origin/Host 白名单校验（既有信任域假设的延伸发现），**列为 M2 发布前加固项** | 已评估不修（留档：①口径留待统一；②M2 发布前加固） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 阶段 5 代审 P2 留档（均低危，评估暂不修）：①WS user-message 无帧大小上限（HTTP 有 1MiB cap，WS 无——本地信任域低危，建议后续加 cap）；②WS 审批响应接受任意本地连接（127.0.0.1 无鉴权信任域的既定边界，requestId 为 UUID 不可预测；建议 architecture.md 显式记录"本地信任域"假设）；③serve 端口锁在 listen 之后获取（--port 0 双实例竞态窗口，输者清场干净，先取锁后 listen 更严谨）；④桌面采纳既有 serve 未校验 root/cwd 一致性（锁文件无 root 字段，采纳异 cwd 实例时会话分组与预期可能不符）；⑤采纳与 spawn 的竞争窗口由退避重启兜底 | 已知限制（留档，暂不修） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **阶段 5 桌面端 GUI 真机验收清单**（自动化只覆盖逻辑路径；以下待 Windows 实机人工）：①`pnpm --filter @harness2/desktop dev` 打开窗口：连接角标转「已连接」、新建会话、mock 消息流式光标与 turn 摘要；②分屏：点「2 栏/3 栏」、从会话列表拖拽到分栏、解绑 ✕、拖拽悬停高亮；③后台会话：分栏外会话跑长任务（bash sleep）时列表徽标计数、绑回分栏后历史快速重放且流未断；④审批：config write=ask 时桌面端弹允许/拒绝条；⑤断线重启：任务管理器杀 serve 子进程 → 角标「重连中…」→ 自动恢复；⑥undo/redo 按钮路径与文件快照联动 | 待处理（需人工实机核对） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **nsis 安装包实机流程**：`packages/desktop/release/harness2 Setup 0.1.0.exe`（112 MB，unsigned）安装/启动/卸载全流程、Windows SmartScreen 对 unsigned 包的提示处理、卸载是否残留用户数据（~/.harness2 会话/布局属用户数据设计上保留） | 待处理（需人工实机核对） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **桌面端真实 provider 端到端**：桌面 UI（对话/流式/审批/undo）接真实模型全流程——与「M1 真实模型 chat 手工验收清单」合并执行（配置 key 后同一轮做）；serve 无配置启动会报错退出（与 chat 一致），桌面端需先有可用 config | 待处理（需人类操作：提供 key） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **npm 发布待授权**（M1 v0.1）：包名 `harness2`（CLI）+ `@harness2/core` 已备好发布物料（files/engines/publishConfig、CHANGELOG、README、`.github/workflows/release.yml` tag 触发）。执行前待人类：①创建远程仓库并授权 push；②在 npm 检查包名占用（`npm view harness2` / `npm view @harness2/core`）；③配置 Actions secret `NPM_TOKEN`；④推 tag `v0.1.0` 触发 release workflow（NPM_TOKEN 缺失时 publish 步骤**条件跳过**并输出明确 notice，不会误发；已配置但发布失败 → workflow 红——2026-09-06 审查修复 P1-1：删除 continue-on-error 假绿路径） | 待处理（需人类操作：授权 + NPM_TOKEN） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **M1 真实模型 chat 手工验收清单**（用户配 key 后逐项核对，命令级）：①`harness2 config check` key 来源正确 → ②`harness2 chat`（roles.main）发一条消息看 SSE 流式直写与 turn 摘要行 → ③让模型写/改文件：`> write (...)` / `< ok [callId]` 行与文件真实落盘 → ④`/undo --dry-run` 预览含文件清单 → `/undo` 文件复原（创建的文件被删除）→ `/redo` 内容回放 → ⑤审批 ask：`config.approval` 下 write 触发 `允许执行 <tool>? [y/a/n]`，`a` 后本会话不再提示 → ⑥`/sessions 关键字` 搜索命中、`/exit` 干净退出。DeepSeek/GLM/Anthropic 三端各过一遍（协议层已由 stub 全覆盖，见下行阶段 3 条目） | 待处理（需人类操作：提供 key） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **Anthropic `pause_turn` 续跑（阶段 4 评估结论：推迟，不实现）**。评估依据：①pause_turn 是 Anthropic 服务端长时操作（server tools/长运行工具）的暂停信号，续跑需把含 pause 块的原始 content blocks 原样回传重发，与现有「每 step 从日志投影重建 ChatRequest」的上下文组装模型不同构——需要在日志与请求映射层为「原始块回传」开新通道，改动面大于收益；②当前产品形态（CLI 单请求 turn）真实触发概率低：主流用法不涉及服务端长时工具；③现有行为已如实安全：turn 以 `paused` 结束 + `TurnResult.warning` 告知 + 日志完整（用户重新发消息即可继续，上下文经投影无损）。重启条件：引入 Anthropic server-side tools / 长时任务（Ph8 subagent 与 Ph7 定时任务附近）时再实现「写回暂停原因并续发同一 turn」 | 已评估推迟（留档，重启条件见左） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 阶段 3 真实端点手工验证：provider 协议层已全部用 127.0.0.1 stub 测试覆盖（零真实 API），但 DeepSeek / 智谱 GLM / Anthropic 三端各「一条真实消息 + 一次工具调用」的实机验证待用户在 `~/.harness2/auth.json` 配置 key 后执行（核对：SSE 流式渲染、tool_calls 组装、`reasoning_content`/`thinking` 展示、usage 统计、错误脱敏）。执行方式：`harness2 config check` 确认配置与 key 来源 → `harness2 chat` 跑一轮（与上方 M1 清单合并执行） | 待处理（需人类操作：提供 key） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 快照恢复的已知边界（阶段 4 实现口径，非缺陷）：①undo/redo 之间若发生了新的 write/edit（有快照条目），redo 的冲突检测会标记 externallyModified——恢复本身是最新 after 的幂等写，无数据风险；②快照条目缺失（崩溃/未 commit）的文件在恢复时静默跳过（不中断整体恢复），日志不受影响；③bash 副作用不进快照已在 chat /help 与 README 如实声明 | 已知限制（记录，留档） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 阶段 3 独立审查 P2-8（评估不修）：SSE 规范允许把单个事件的 data 拆成多行 `data:`（拼接后为一个 JSON），本实现按单行 `data:` 帧解析——多行 data 帧的后续行 JSON.parse 失败被当作坏帧跳过。影响与触发条件：仅当服务端把单个 JSON 事件拆到多行 `data:` 发送时丢帧（主流端 DeepSeek/GLM/Anthropic 实测均为单行，未发现触发路径）；最坏后果是该事件内容缺段或报 `stream_truncated`，不会产生错误内容。待出现真实多行端点再实现跨行 data 拼接 | 已评估不修（留档） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 审查 P2-3（剩余）：多进程并发追加、陈旧锁 TOCTOU、父目录刷新未实现（fsync 默认路径冒烟测试已于阶段 2 补齐） | 部分关闭（带入后续阶段） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 阶段 2 独立审查 P2-6：reader 容错口径问题——复核评估为无害行为，决定不修（留档备查） | 已评估不修（留档） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 审查 P2-6d 留档（多级 undo 后逐级 redo 的中间态口径）：redo 的文件恢复范围 = 被 redo 的 undo 所撤的全部条目（seq > 该 undo 的 rewindToSeq 取最新 after），第一次 redo 可能把尚未复活的更外层 turn 的文件改动一并恢复——出现「文件领先于对话」的中间态，继续 redo 至对话追平后一致，恢复了哪些文件在 redo 输出中如实列出。实现维持此口径不改，详见 architecture.md「文件快照与 undo/redo」恢复语义条目 | 已知限制（留档，实现不改） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | 审批内联提示的输入约定（P2-2 修复后口径）：ask 等待期间下一行输入即答案（含 `/exit` 等以 / 开头的行也会被当答案、按拒绝处理）；取消等待用 Ctrl+C / Ctrl+D——ask 与 turn 取消信号竞速，abort 后以「审批等待被取消（按拒绝处理）」结束且不再吞下一行输入（chat-cancel.test.ts 经 \x03 注入 SIGINT 覆盖）。真机 tty 手感并入 Windows Terminal 人工核对 | 已知限制（留档） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | **Windows Terminal 实机 REPL 体验待人工**：`── turn` 标头渲染、颜色/光标/长行折行、审批内联提示与 Ctrl+C 取消（含审批 ask 等待中 Ctrl+C）的实机手感——自动化仅覆盖逻辑路径（piped/进程内测试），真机 tty 行为待用户在 Windows Terminal 逐项核对（可并入下方 M1 手工验收清单执行） | 待处理（需人工实机核对） | [2026-09-06.md](2026-09-06.md) |
| 2026-09-06 | CI 待远程验证：`.github/workflows/ci.yml` 三平台 test matrix + build-desktop 桌面包构建 matrix 已建并通过本地 YAML 语法校验（2026-09-06 审查修复 P1-1：删除 version 输入改由 packageManager 驱动，待远程一并验证；阶段 11 增 build-desktop 三平台构建与 release.yml 三平台产物附加 GitHub Release，Windows 本地 nsis 构建已通过——另见 2026-09-07 三平台 CI 产物待远程验证行），Actions 真实运行需远程仓库 + push 授权（需人类操作）。`release.yml` 同过 js-yaml 本地语法校验（2026-09-06 审查修复 P1-1 后复验：publish 步骤改 NPM_TOKEN 条件跳过 + 失败即红），真实运行同待远程 | 待处理（需人类操作） | [2026-09-06.md](2026-09-06.md) |
