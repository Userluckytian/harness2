# Grok 终端 12 场景对照清单（仅源规格，视觉验收待真机签收）

> 来源：`docs/research/notion-ai-20260908-0056/04-implementation-plan.md` §5.3。
> 状态：**仅源码规格 + 自动化逻辑路径**；终端视觉/手感差异留用户真机签收（验收 A11-T）。
> 评分口径（§5.3）：每场景按「布局层级 / 操作结果 / 状态反馈」三项 0/1；TUI 轨 T0-T5 实现后逐项核。
> 本清单是 S0 冻结的对照锚点，不是实机录屏结果——不得以换色/卡片数量替代交互验收。

| #   | 场景           | 布局层级(0/1) | 操作结果(0/1) | 状态反馈(0/1) | 备注（当前基线对应差异）                                                                                   |
| --- | -------------- | ------------- | ------------- | ------------- | ---------------------------------------------------------------------------------------------------------- |
| 1   | **空闲**       | □             | □             | □             | 提示/历史/状态栏基线：现有 TUI `runInkChat` 静态历史 + 状态行；Composer 忙时禁输入（审计 H1）              |
| 2   | **长草稿**     | □             | □             | □             | 多行编辑/自动高度存在（02 调研），grapheme 光标/词移动缺失（H2 范围），视觉待签收                          |
| 3   | **候选**       | □             | □             | □             | 命令/参数候选面板无（T1 范围）；`/help` 文字列表为现状                                                     |
| 4   | **引用**       | □             | □             | □             | 结构化引用（`MessageReference`，S0 契约）已冻结，引用 UI 卡片属 TUI 轨                                     |
| 5   | **多行 paste** | □             | □             | □             | 粘贴 chip/CRLF/1MB 限额属 T2；含 `/exit` 的 paste 不得自动触发命令为硬约束                                 |
| 6   | **文本流**     | □             | □             | □             | `WS delta` 50ms 缓冲已在（useTurnStream）；delta 归属 turnId/attemptId/chunkOffset 由 S0 契约冻结、S3 接线 |
| 7   | **工具进行中** | □             | □             | □             | tool-call/result 事件已镜像；可展开工具卡、持久历史属 T3                                                   |
| 8   | **审批**       | □             | □             | □             | 内核审批链已存在（requestId→settle）；结构化批准契约（scope/expiresAt/decision ack）S0 冻结、S2 接线       |
| 9   | **子任务并行** | □             | □             | □             | subagent 工具存在；background handle + status/wait/cancel + K=2 只读并行属 S5；子审批父可见属 S2           |
| 10  | **错误/重试**  | □             | □             | □             | 失败 attempt 保留完整、可见（审计 H3）；有界重试/429/Retry-After 分类 S0 冻结、S4 实现                     |
| 11  | **长历史**     | □             | □             | □             | Static 全量渲染、非虚拟化（审计 H2）；滚动锚点/历史卡片展开属 T3                                           |
| 12  | **切会话**     | □             | □             | □             | 会话切换全量重投影存在；resumeSubscription 水位/epoch 契约 S0 冻结、S3 接线                                |

## 说明

- 本表在 S0 仅登记「规格/契约锚点」；每项 0/1 由 TUI 轨（T0-T5）与对应共享 Task（S2-S6）交付后由各自任务证据更新。
- 「核心输入/取消/审批/恢复四类满分，整体 ≥90%，剩余差异显式签收」的总闸门在 A11-T。
- 可运行的 Grok 参考对着录屏（上游 commit + 终端尺寸/缩放/主题）时另建 `S*-grok-ref.md`；无参考时保持本清单标注「仅源码规格」。
