// 会话视图环（D-32）的**已知视图 key** —— 唯一字面量来源。
//
// 为什么单独一个文件：视图 key 是「谁贡献了哪个视图」与「工具卡 inspect 跳哪里」之间的
// **共享契约**，两侧都要引用同一份字面量（两处写字面量必然漂移）。本文件零依赖，任何一层
// 都可以安全引用，不产生环。

/** 内置主视图（D-31 的回落目标；每个壳都必须注册它才谈得上「有默认视图」） */
export const CHAT_VIEW_KEY = 'chat';

/** 轨迹视图（D-40；由具备轨迹能力的壳贡献，未贡献时工具卡不渲染 inspect 入口） */
export const TRAJECTORY_VIEW_KEY = 'trajectory';
